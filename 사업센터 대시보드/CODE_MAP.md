# CODE_MAP.md — index.html 코드 위치 지도

> 목적: `index.html`(한 파일에 전체 기능이 들어있음, 1만 6천 줄 이상)에서 **어떤 화면을
> 고치려면 어느 함수를 찾아야 하는지** 빠르게 알기 위한 지도. 코드 설명서가 아니라
> "어디를 열어야 하는가"만 다룬다. 줄 번호는 파일이 바뀌면 곧 틀어지므로 적지 않는다 —
> 대신 grep으로 바로 찾을 수 있는 **함수명·문자열**을 적는다.

- 버전: v01.74
- 최초 작성일: 2026-09-08
- 관련 문서: `MD_ROUTER.md`(정책·데이터 문서 안내), `PROJECT_CONTEXT.md`(현재 상태·이력)

## 유지보수 규칙 (중요, CLAUDE.md 8번 섹션 11번 항목과 동일)

**코드를 수정하거나 화면·함수를 추가·삭제했으면 이 문서도 반드시 갱신한다.** 예외를
임의로 판단하지 않는다 — 안 갱신하면 다음에 더 헷갈리게 만드는 낡은 지도가 된다.

---

## 1. 전체 구조 3줄 요약

1. 왼쪽 사이드바 메뉴는 `NAV` 배열(사이드바 섹션·항목 정의) → 클릭하면 `goTab(id)` →
   `TAB` 전역변수를 바꾸고 `paintSide()`(사이드바 다시 그림) → `route()`(오른쪽 화면 그림) 순으로 이어진다.
2. `route()`는 `TAB` 값을 보고 그 화면을 **3가지 방식 중 하나**로 그린다(아래 2번 참고) —
   같은 이름의 화면이라도 어떤 방식인지에 따라 실제 코드 위치가 완전히 다르다.
3. 저장·복원(새로고침해도 안 사라지게)은 **두 계층**으로 나뉘어 있고(6번 참고), 둘 다
   "무엇을 저장할지" 목록에 새로 등록해야만 실제로 저장된다 — 등록을 빠뜨리는 게 이 코드베이스
   에서 가장 자주 반복된 실수다(PROJECT_CONTEXT.md "13. 배운 점" 참고).

---

## 2. `route()`가 화면을 그리는 3가지 방식

`route()` 함수(`function route(){`로 검색) 안에서 `TAB` 값을 위에서부터 순서대로 검사한다.
**먼저 걸리는 조건이 그 화면을 그린다** — 뒤에 같은 id를 검사하는 코드가 또 있어도 절대
실행되지 않는다(3번 "알려진 함정" 참고).

| 방식 | 판별 조건 | 실제로 화면을 그리는 곳 |
|---|---|---|
| **① IX 시스템** (원본 index.html을 통째로 이식한 13개 화면) | `IX_TABS.indexOf(TAB)>=0` | `ixShell(TAB)`으로 틀만 만들고 `ixMount(TAB)`이 원본 탭바 버튼을 대신 클릭 + `RENDER[TAB]()`(=`window.__ixRender[TAB]()`)를 호출해 다시 그린다. **진짜 내용은 `RENDER['그화면id']=...` 로 등록된 함수 안에 있다.** |
| **② 영업 대시보드 어댑터** | `TAB==='sales'` | `salesShell()`+`salesMount()` — `#salesHost`/`#salesKeep`/`#salesSlot`으로 별도 영업용 대시보드를 붙였다 뗀다. IX 시스템과 다른 독자 구조. |
| **③ 단순 화면** (이 목업 전용으로 새로 만든 화면) | `TAB==='act'`, `'updates'`, `'mkt'/'biz'/'ux'`, `'sem'`, `'week'` | `route()` 안에 `p.innerHTML=OOShell(); paintOO();` 한 줄로 바로 그린다. `TABS`·`IX_TABS`·`STUB` 같은 4종 세트 없이 `NAV`+`BUILT_TABS`+이 한 줄만 있으면 된다. **새 화면을 추가할 때는 이 방식이 제일 간단하다.** |

`analyticsShell`/`homeShell`/`inqShell`로 시작하는 `else if(TAB==='home') ...` 계열 코드가
`route()` 맨 아래에 남아 있는데, `home`·`daily`·`monthly`·`cumul`·`inq`는 전부 ①(IX_TABS)에서
먼저 걸려 `return`하므로 **이 아래쪽 분기들은 죽은 코드**다(3번 참고).

---

## 3. 화면별 담당 함수 — 사이드바에 보이는 순서대로

각 행의 "찾는 법"은 그대로 grep에 붙여넣으면 되는 문자열이다.

| 사이드바 표시명 | tab id | 방식 | 실제 담당 함수 (찾는 법) | 비고 |
|---|---|---|---|---|
| 주간회의 | `week` | ③ | `function paintWeek(){` / `function weekShell(){` | 월별 표 2개는 `monPerfTableHtml`(플러스·프로모션 가입)·`planJoinTableHtml`(전체 가입 현황) — 해당 달 열 형광펜은 두 표가 공유하는 `wkMonHl(y,m,strong)`(v08.281) — **기준은 보고 있는 주간회의 주(`CURWK`)이고 귀속 달은 주 종료일이 속한 달**(v08.304, 목~수 주간이라 7/30~8/5는 8월). `CURWK`가 없으면 오늘 날짜로 폴백한다, **열 폭·정렬은 두 표가 공유하는 `wkMonColgroup()`+인라인 `table-layout:fixed`**(v08.283, 아래 11번 참고). 팀별 블록은 `teamBlockHtml`(핵심과제·추진현황·주간 업무 현황) → 주간 업무 현황 2단 표는 `teamWeekTableHtml`(**이번 주 / 다음 주**, v08.315 · 과제 제목 옆 **📎는 그 카드의 `DOCS[카드id]` 링크**, v08.317 — v08.283의 「저번 주/이번 주」를 되돌림. 그 주에 실제로 찍힌 실행기록만 세므로 다음 주 칸은 기록이 생기기 전까지 비어 있다). PDF 저장은 `@media print` CSS(파일 상단)로만 제어. `RENDER['week']`도 등록돼 있지만 route()는 안 거쳐가고 `paintWeek()`를 직접 부른다. |
| 통합 현황 | `home` | ① | `RENDER['home']=render;` 바로 위 IIFE, `document.getElementById('s-home')` | 화면 아래쪽 `homeShell()`/`paintHome()`은 **죽은 코드**(호출 안 됨). |
| 시장 현황 | `sales` | ② | `function salesMount(){` | |
| 일별 | `daily` | ① | `function renderDaily(){` (`document.getElementById('s-daily')`) | **중복 주의**: 파일 앞쪽에 옛 `render()`(같은 `#s-daily`)가 먼저 있는데 `RENDER['daily']`가 나중에 `renderDaily`로 다시 등록돼 덮어쓴다 — **`renderDaily`가 실제로 화면에 보이는 쪽**이다. 앞쪽 옛 `render()`를 고쳐도 반영 안 된다. |
| 월별 | `monthly` | ① | `function renderMonthly(){` (`document.getElementById('s-monthly')`) | 위와 같은 중복 구조 — `renderMonthly`가 활성. |
| 누적 | `cumul` | ① | `function renderCumul(){` (`document.getElementById('s-cumul')`) | 위와 같은 중복 구조 — `renderCumul`이 활성. |
| 실적 관리 | `perfmg` | ① | `function renderPerfMg(){` (`document.getElementById('s-perfmg')`) | 중복 없음. **첫 진입 시 등록일 기간이 「오늘이 속한 달」로 채워진다**(v08.321 · `__pmF._initMo`로 한 번만, 그 뒤로는 사용자가 고른 기간 유지. 파일 기준일이 오늘보다 뒤면 그 기준월을 쓴다. ※ `TODAY` 상수는 이 코드보다 아래에서 선언되므로 여기서는 `new Date()`로 직접 만든다). 표는 문의 · 요금제 · **유입경로 · 가입채널 · 최종실적** 5열이고 하위 탭 2개(문의 있는 건 `renderPerfMg` / 문의 없는 실적 `pmOrphanHTML`). 툴바에 패널 3개 — `inflowPanelHTML`(유입경로 관리) · `refPanelHTML`(추천 정보 관리) · `protPanelHTML`(실적확정 명단). **패널도 `renderPerfMg`가 매번 다시 그린다** — 저장 후 목록을 새로 그리면 패널이 닫히므로 다시 열어 줘야 한다(`protSave` 참고). **※ v08.318·v08.319에서 「유입경로를 신청 관리로 이동」·「두 하위 탭 합치기」를 했다가 v08.320에서 전부 원복했다** — 근거: 플로우 도입문의에는 요금제 상향 내용만 적재돼 유입경로는 실적 관리에 있는 편이 유의미하다(CLAUDE.md 8-13 참고, 재요청 시 확인 필수). |
| 신청 관리 | `inq` | ① | `RENDER['inq']=render;` 바로 위 IIFE, `document.getElementById('s-inq')` | **조회 전용**이다(v08.98) — 유입경로·가입채널·최종실적 지정은 실적 관리에서 한다. v08.318에서 유입경로 지정을 여기로 옮겼다가 **v08.320에서 원복**했다(CLAUDE.md 8-13). |
| 마케팅 / UX / 영업 | `mkt`/`ux`/`biz` | ③ | `function shell(){` / `function paint(){` | 3개 팀 탭이 **같은 함수 하나**를 공유하고 내부에서 `TAB` 값으로 분기한다. 팀탭 공통 UI(핵심과제 등)를 고칠 땐 여기. 「성과 지표」 블록은 `teamMetricPanelHtml`(표·입력칸)·`metricChartSvg`(막대 그래프)·`paintTeamMetric`. SVG는 실제 크기(width/height 속성)로 그린다 — `width:100%`만 주면 값이 몇 개 없을 때 통째로 확대된다(v08.280). **카드 상세 패널은 `paintPage()`**(`#pgBody`) — `openPage(cardId)`로 연다. 그 안 「개요」·「진행 방식」은 둘 다 `textarea.ta`(min-height 52px)인데, **「진행 방식」만 `.ta-lg`(168px ≈ 9줄)를 덧붙여 기본 높이를 키웠다**(v08.355 요청 — 단계·순서를 여러 줄로 적는 칸이라 매번 드래그해 늘려야 했다. `resize:vertical`은 그대로). |
| 세미나 운영 | `sem` | ③ | `function semShell(){` / `function paintSem(){` | |
| 프로모션 종료·정상가 전환 | `promo` | ① | `RENDER['promo']=render;` 바로 위 IIFE, `document.getElementById('s-promo')` | "종료 관리 목록"·"190만 일시납" 관련 로직이 전부 이 안에 있다. |
| 해지·재가입 | `churn` | ① | `function churnRender(){` (`document.getElementById('s-churn')`) | `opsPage()` 공통 틀(카드+목록) 사용. |
| 정지업체 | `suspend` | ① | `function suspendRender(){` (`document.getElementById('s-suspend')`) | 위와 같은 `opsPage()` 틀. 목록은 `suspendRowsAll()`(마스터 상태='정지') → `spPaint()`가 그린다. 열은 **업체명 · 대표자 · 연락처 · 요금제 · 정지 사유 · 정지일 · 정지해제일 · 메모**(v08.320 — 메모는 `__spMemo`(STATE_MAP `sp_memo`)에 사업자번호 키로 저장, 입력 즉시 저장하고 목록은 다시 그리지 않는다)(v08.316 — 대표자 `ow`·연락처 `hp`는 마스터가 이미 담고 있던 값. 검색창에서도 찾히며 휴대폰은 숫자만 입력해도 매칭). |
| 고객 검색 | `cust` | ① (변형) | `window.custRender=function(){` (`document.getElementById('s-cust')`) | 다른 IX 화면과 달리 `RENDER[]`가 아니라 `window.custRender`로 노출 — 탭을 다시 열 때 자동 재호출(`ixMount`)이 안 걸릴 수 있으니, 값이 안 바뀌면 이 차이부터 의심할 것. |
| 데이터 업로드 | `data` | ① | `document.getElementById('s-data')` 를 채우는 IIFE (`xlPanelHTML()` 포함) | v08.272부터 사이드바 메뉴 자체가 `jennie.gil@roumit.com` 로그인일 때만 보임(`paintSide()`의 `DATA_UPLOAD_EMAIL` 필터). 엑셀 파서 본체는 `function xlParseOffice`/`xlParseBilling`/`xlParseMonitor`/`xlApplyInq`. |
| 활동 타임라인 | `act` | ③ | `function actShell(){` / `function paintAct(){` | v08.275부터 사이드바 "안내" 섹션 소속(예전엔 "요약"). |
| 업데이트 내역 | `updates` | ③ | `const UPDATE_LOG=[` / `function paintUpdates(){` | 새 업데이트를 추가할 땐 `UPDATE_LOG` 배열 **맨 앞에** `{id:'다음 번호', d:'날짜', tt:'제목', b:'쉬운 설명'}` 추가(id는 안 읽음 배지 판정 기준이라 한 번 쓰면 절대 재사용·수정 금지). |

---

## 4. 사이드바 메뉴 구조 자체를 고칠 때

- 메뉴 목록·섹션·순서: `const NAV=[` 배열.
- 메뉴 항목 옆 건수 배지·"NEW" 배지: `function paintSide(){` 안의 `.map(i=>{...})` 부분.
- 검정 글씨(화면 있음) vs 회색 글씨(화면 없음) 구분: `const BUILT_TABS=[` 배열 — 새 화면을
  만들면 여기에도 id를 추가해야 진하게 보인다.
- 탭이 없을 때 보여줄 안내문(스텁): `const STUB={`.

---

## 5. 계정별 화면 노출 제어

- 로그인 이메일은 `showUser()`가 `window.__curEmail`에 저장한다(로그인 성공마다 갱신).
- "이 계정만 보이게" 만들고 싶으면 `paintSide()`의 `sc.items.filter(...)`(메뉴 자체를 숨김) 또는
  `showUser()` 안의 버튼 조립 부분(버튼만 숨김, 예: "설정")을 참고해 같은 패턴
  (`window.__curEmail===특정이메일`)을 재사용한다. 이미 `DATA_UPLOAD_EMAIL`(현재
  `jennie.gil@roumit.com`) 상수가 있으니, 같은 계정 기준이면 새 상수를 또 만들지 않고 재사용한다.

---

## 6. 저장·복원 — 두 계층 (등록 안 하면 저장 안 됨)

| 계층 | 저장 함수 | 목록(반드시 등록) | 복원 함수 | 주로 다루는 데이터 |
|---|---|---|---|---|
| IX 계층 | `IX_saveToSheet(key, 값)` | `const STATE_MAP={` | `IX_loadFromSupabase()` | 업로드 파일(세무사무소목록·청구내역 등), 통합현황류 통계 |
| 보드 계층 | `saveToSheet(key, 값)` | `const BOARD_STATE_MAP={` | `loadFromSupabase()` | 칸반 카드(`CARDS`)·팀 핵심과제·업무보드·사용자 명부(`__userDir`)·팀탭 성과 지표 등 |

새 전역 상태(`window.__새변수` 등)를 만들어서 새로고침 후에도 남아있어야 한다면, **어느 계층이든
반드시** 위 두 `_STATE_MAP` 중 하나에 `저장키:'전역변수명'` 한 줄을 추가하고, 그 계층의 복원
함수에도 복원 분기를 추가해야 한다. 둘 다 안 하면 화면엔 잘 나오다가 새로고침·재배포하면
조용히 사라진다 — 이 프로젝트에서 가장 많이 반복된 버그 유형이다.

---

## 7. 입력 칸 공통 규칙 — 날짜(v08.277)·검색(v08.278)

`type=date` 칸은 연·월·일이 따로 나뉘어 있어서 **「30」을 치려고 「3」만 눌러도 그 순간 값이
유효해지고(2026-09-03) change가 먼저 난다.** 그때 화면을 다시 그리면 입력 중이던 칸이 새로
만들어져 커서가 사라지고, 뒤이어 친 「0」이 갈 곳을 잃는다 → 「30」이 「03」으로 굳는다.
`type=month`도 연도 칸에서 같은 일이 난다(「2」만 쳐도 0002년으로 유효해진다).

**그래서 날짜 칸의 onchange에서 화면을 다시 그릴 때는 반드시 `dtRedraw(그릴함수)`로 감싼다.**
(`const RENDER={}` 선언 바로 아래에 있는 공통 블록 — `dtRedraw`로 grep)

- 커서가 그 칸에 있는 동안(직전 1.5초 안에 키를 눌렀으면)은 **다시 그리지 않고 미룬다** —
  칸을 벗어날 때(blur), 또는 커서를 계속 둬도 2초 뒤에 그린다.
- 달력에서 골랐거나 코드가 값을 바꾼 경우(키 입력 없이 change)는 **즉시** 그린다.
- 시작~끝 칸을 Tab으로 옮겨 이어 입력하는 경우도 그 칸까지 끝날 때까지 미룬다.
- blur 도중에 다시 그리면 브라우저가 `innerHTML` 오류("옮겨진 노드")를 내므로 한 틱 뒤에 그린다.
- 어느 칸인지 넘길 필요 없다 — change 시점에 그 칸이 `document.activeElement`다.
- 키 누른 시각은 문서 전체에 걸어 둔 keydown 리스너가 자동으로 기록한다. **새로 만드는 날짜
  칸에는 아무것도 달지 않아도 되고, 재렌더 함수만 `dtRedraw`로 감싸면 된다.**

적용된 곳(이 규칙을 따르는 setter): `pmSet`(실적 관리 발생일·등록일) · `ofSet`(신청 관리 ›
고객 현황 기간) · `inqSetVal`(신청 관리 등록일) · `daySet`(일별) · `monSet`(월별) ·
`mtMonthSet`(마케팅 정기업무 기준월) · 카드 상세는 `setF`/`setLog`가 v08.164에서 다른 방식
(패널을 아예 다시 안 그림)으로 이미 해결.

**기간 필터를 한 달씩 옮기는 버튼**은 `dtMonthRange(기준일, ±1)`(그 달 1일~말일을 돌려준다)로
만든다 — 화면별 래퍼는 `pmMonthShift`(실적 관리) · `ofMonthShift`(고객 현황). 일별·월별·기준월은
그 전부터 있던 `dayShift`/`monShift`/`moShift`를 쓴다.

### 검색 칸(글자 입력)도 같은 문제 — `txRedraw` — v08.278

검색 칸은 `oninput`(글자 하나마다)으로 화면을 다시 그리던 곳이 있었다. 그러면 그 칸이 새로
만들어져 커서가 사라지고, **한글은 조합 중이던 글자까지 끊겨 첫 자모만 남는다**(「무선」을
치면 「ㅁ」만 들어갔다). 그래서 검색 칸에서 화면을 다시 그릴 때는 `txRedraw(그릴함수, '칸 id')`를
쓴다 — ① 마지막 입력 뒤 0.3초에 한 번만 그리고 ② 한글 조합 중에는 조합이 끝날 때까지 미루고
③ 그린 뒤 그 칸을 id로 다시 찾아 커서를 원래 자리로 되돌린다. **그래서 검색 칸에는 id가 있어야
하고, 마크업에서 `this.id`를 넘겨준다.**

적용된 곳: `pmSet('q',…)`(실적 관리 두 하위 탭 — `#pmQ`·`#pmQorp`) · `ofSet('q',…)`(신청 관리 ›
고객 현황 — `#ofQ`) · `inqSetVal('iq',…)`(신청 관리 제목 검색 — `#inqHdrQ`).

**반대로, 애초에 목록만 다시 그려서 이 문제가 없는 곳은 건드리지 않았다** — `chSet`→`chPaint()`
(`#chList`만), `spSet`→`spPaint()`(`#spList`만), 고객 검색 `custRender()`→`#cList`·`#cCnt`만,
사이드바 검색 `sideFilter()`(보이기만 토글). **이게 원래 더 나은 구조다** — 새 검색 칸을 만들 때는
가능하면 이 방식(입력 칸이 든 영역은 그대로 두고 목록만 갈아끼우기)을 먼저 쓰고, 화면 전체를
다시 그려야만 하는 경우에 `txRedraw`를 쓴다.

## 8. 실적 귀속 — 두 개의 "실적확정 명단" (v08.279)

사업자번호가 명단에 있으면 **유입경로·가입채널·최종실적 세 칸이 자동 확정**되고, 실적 관리
목록에서 **테두리 색**으로 구분된다. 두 명단은 구조가 완전히 같다.

| 명단 | 전역변수 / 저장키 | 판정 함수 | 적용 채널값 | 테두리 |
|---|---|---|---|---|
| 위멤버스 실적확정(실적보호업체) | `__wembProtect` / `wemb_protect` | `wembProtectHit(r)` | `위멤버스 > 온라인 마케팅` | **빨강** `var(--red)` |
| 로움 실적확정 | `__salesInside` / `sales_inside` | `salesInsideHit(r)` | `영업 > 내부영업` (`window.SALES_INSIDE_CH`) | **파랑** `#3B82F6` |

- 판정은 **사업자번호 완전일치만** — 상호명으로는 판정하지 않는다(짧은 상호가 다른 사무소 문의에
  우연히 들어가면 실적이 통째로 잘못 귀속된다, v08.98).
- **두 명단에 같은 번호가 있으면 위멤버스가 우선**한다(`salesInsideHit`이 내부에서 먼저 걸러낸다).
- 값을 계산하는 자리 3곳: `inflowBase`(유입경로) · `pmRows`의 `joinCh`(가입채널) ·
  `IX_creditChannel`(최종실적). **수동으로 고른 값이 있으면 항상 그것이 우선**한다(잠그지 않는다).
- 「문의 없는 실적」 탭은 드롭다운 대신 배지(「위멤버스 확정」/「로움 확정」)로 보여주고 잠근다.
- 일괄 지정(`pmBulkTargets`)에서는 두 명단 건을 **대상에서 제외**한다(이미 확정값이라).
- **등록·삭제 화면(v08.284)**: 실적 관리 화면 툴바의 **[실적확정 명단]** 버튼 → `#protPanel`.
  `window.protPanelHTML()`(패널) / `protBlockHTML(which)`(명단 한 벌) / `protAdd`·`protDel`·`protClear`
  / `protParse`(붙여넣기 파싱) / `protSave`(저장 + `RENDER['perfmg']()` 재렌더 + 패널 다시 열기).
  두 명단의 차이는 `window.PROT_DEF`(kind·전역변수명·이름·색·채널값·textarea id) 한 곳에만 있다 —
  명단을 더 늘릴 때도 이 표에 한 줄 더하는 식으로 확장한다.
  붙여넣기 파싱 규칙: 한 줄에 한 곳, 줄에서 **8자리 이상 숫자 덩어리**를 사업자번호로 보고
  (하이픈·공백 허용) 나머지 글자를 상호명으로 본다. 사업자번호를 못 찾은 줄은 **건너뛴다**.

## 9. 플로우 엑셀의 하위업무 (v08.282)

플로우 엑셀에서 **하위업무는 '내용' 칸 속 텍스트가 아니라 자기 업무번호를 가진 별도의 행**으로
내려온다. 부모와의 연결은 **상위업무 컬럼**(부모 업무 제목)에 들어 있다.

- 파서 `xlParseTasklist`가 `xlIdxLoose`로 상위업무 컬럼을 느슨하게 찾아(`상위업무`·`상위 업무`·
  `상위업무명`·`부모업무` 등, 번호 컬럼도 함께 시도) 각 행에 `pt`(부모 제목)·`pno`(부모 번호)로 담는다.
  인식 결과는 `window.__flowSubCol`(찾은 컬럼명·못 찾았을 때의 헤더 목록)에 남는다.
- `xlLinkSubTasks(rows)`가 부모를 찾아 `window.__inqSubs`(부모 fid → 하위업무 배열)를 만든다.
  연결 기준은 ① 상위업무번호 ② 상위업무 제목(공백 무시). 제목은 파서가 `[유형] 제목`으로 쪼개
  두므로 비교할 때 다시 붙여 맞춘다.
- **하위업무 행은 문의가 아니다** — `inqCls`가 `'sub'`를 돌려주고, 테스트·공지와 같이 실적 집계에서
  빠진다(`isPerfTarget`). v08.282 전에는 제목에 대괄호가 없다는 이유로 「도입 문의」로 세어
  문의 건수를 부풀리고 있었다. `inqCls` 반환값을 키로 쓰는 맵(`{inq:[],join:[],…}`)에는
  **`sub:[]`를 반드시 넣어야 한다** — 없으면 `push`에서 오류가 난다.
- **하위업무 제목이 추천인코드 모양**(`SUB_REF_RE` = 영문 대문자·숫자·밑줄/하이픈, 예 `WE_MOON`)이면
  부모의 가입채널을 확정한다. 어떤 값을 쓸지는 **`refEnsureChannel(code)` 한 곳에서만** 정한다(v08.283):
  ① 추천 정보 관리(`window.__refList`)에 그 코드로 적어 둔 추천채널이 있으면 **그 값**(`refChannelOf`),
  ② 없으면 「영업 > 코드」를 쓰고 **같은 내용을 추천 정보 관리에 자동 생성**한다(행이 없으면 새 행,
  행은 있고 채널 칸만 비었으면 그 칸만 채움 — 추천인 등 다른 칸은 건드리지 않는다).
  사람이 수기로 지정한 가입채널은 덮어쓰지 않는다.
  자동 생성분 저장은 행마다 하지 않는다 — `window.__refDirty`만 켜 두고 `xlLinkSubTasks`가 끝날 때
  `refSave()`를 **한 번만** 부른다(저장 요청·`#refPanel` 재렌더 폭주 방지).
  ※ 이로써 '내용' 텍스트에서 추천인코드를 찾는 옛 경로(`xlAutoJoinChFromContent`, v08.98/v08.244)와
  규칙이 거의 같아졌다 — 다른 점은 옛 경로는 **매핑이 없으면 자동지정을 포기**한다는 것(자동 생성 안 함).
- **새로고침·재배포 후 복원(v08.284)**: 저장되는 것은 **행(`inq_rows`)** 뿐이고 부모-하위 연결
  (`__inqSubs`)은 업로드할 때만 계산되는 파생값이라, 예전에는 화면을 새로고침하면 하위업무 표가
  사라지고 「상위업무 컬럼이 없어 연결할 수 없습니다」라는 잘못된 안내가 떴다(= "배포하면 파일이
  삭제된다"고 보이던 증상). 복원된 행에 `pt`·`pno`가 남아 있으므로 `IX_loadFromSupabase`가
  **`window.xlRelinkSubsAfterRestore()`** 를 불러 연결만 다시 만든다 —
  이때 `xlLinkSubTasks(rows, true)`(linkOnly)로 불러 **가입채널 확정·추천 정보 자동 생성·저장은
  하지 않는다**(그 결과는 업로드 때 이미 저장됐고, 복원 직후 저장은 서버 데이터를 덮어쓸 위험).
  컬럼 인식 결과 `__flowSubCol`은 계산으로 되살릴 수 없어 `STATE_MAP`의 `flow_sub_col`로 저장한다.
- 하위업무는 신청 관리 카드 상세(`inqDetail`)에 표로 보여준다. 상위업무 컬럼이 없는 파일이면
  그 사실을 안내한다(없는 값을 지어내지 않는다).

## 10. 알려진 함정 (grep하기 전에 먼저 확인)

1. **`route()` 아래쪽의 `else if(TAB==='home'/'daily'/'monthly'/'cumul'/'inq')` 분기는
   죽은 코드다** — 위쪽 IX_TABS 체크에서 이미 `return`하기 때문에 절대 실행되지 않는다.
   `homeShell`/`paintHome`/`analyticsShell`/`paintDaily`(옛 버전)/`inqShell`/`paintInq`를
   고쳐도 화면에 반영되지 않는다.
2. **`week`(주간회의)도 렌더러가 두 벌이다 — 옛 IX 방식(`#s-week`)은 화면에 보이지 않는다.**
   살아있는 쪽은 `paintWeek()`가 그리는 `#weekBody`이고, `#s-week`(=`#ixHost` 안, `display:none`)에
   있는 옛 화면은 「편집 모드 / 주간보고 모드」 전환(`wkEditTog`)·`.rp-2col`·`③ 주간 업무 현황`을
   가진 **죽은 코드**다. 두 벌이 「주간 업무 현황」·「이번 주/다음 주」 같은 **같은 문구를 공유**하므로
   grep만으로는 구분되지 않는다 — 고칠 곳을 정하기 전에 Playwright로 `display`/`offsetParent`를
   확인한다(2026-09-09 실수: 보이지 않는 쪽을 근거로 사용자에게 불필요한 확인을 요청했다,
   PROJECT_CONTEXT.md "13. 배운 점" 37번).
3. **`daily`/`monthly`/`cumul`은 각각 렌더 함수가 파일에 두 번 정의돼 있다** — 나중에 정의된
   `renderDaily`/`renderMonthly`/`renderCumul`이 `RENDER[]`에 마지막으로 등록돼 실제로
   화면에 보이는 쪽이다. 고칠 함수가 맞는지 헷갈리면 `RENDER['daily']=`를 grep해서 **몇 번
   나오는지, 그중 어느 줄이 파일에서 더 아래에 있는지** 확인한다(더 아래 것이 이긴다).
4. **같은 이름의 함수·변수가 여러 화면에 나눠 있을 수 있다** — 고치기 전에 항상
   `grep -n "함수명"`으로 정의·호출 위치를 먼저 셀 것(CLAUDE.md 8번 섹션 2번 항목).
5. **전역 상태를 읽기 전에 복원이 끝났는지 확인할 것** — `loadFromSupabase()`/
   `IX_loadFromSupabase()` 완료 전에 어떤 전역값을 읽어 저장을 예약하면, 복원이 끝난 뒤에도
   그 예약이 옛 값으로 서버를 덮어쓸 수 있다("복원 전 계산·저장" 경합조건, PROJECT_CONTEXT.md
   "13. 배운 점" 참고 — 이미 4번 이상 반복된 유형).
6. **입력 칸(날짜·검색)의 핸들러에서 그 칸이 들어있는 영역을 다시 그리면 입력이 끊긴다**
   — 7번 섹션 참고. "연도 4자리 검사"나 "0.4초 디바운스" 같은 임시 방어로는 못 막는다(사람이
   그보다 느리게 치면 그대로 깨지고, 한글은 조합 중 글자가 날아간다). 날짜 칸은 `dtRedraw`,
   검색 칸은 `txRedraw`로 감싸거나, 애초에 목록만 갈아끼운다(CLAUDE.md 8번 섹션 5번 항목).
7. **`#s-mkt`·`#s-biz`(마케팅 정기업무·영업 잠재고객 화면)는 지금 어디에서도 열리지 않는다** —
   `mkt`·`biz` 탭은 팀 보드(`shell()`/`paint()`)로 가고 `IX_TABS`에도 없어서, 그 안의 UI(기준 월
   칸·검색 칸 등)는 화면에 뜨지 않는다(그래서 그 안의 같은 버그는 손대지 않았다 — 확인할 방법이
   없다). 그 화면을 고치라는 요청을 받으면 먼저 이 사실을 알릴 것.
8. **`inqRowOf(fid)`의 색인은 행 수로 무효화한다** — 이 색인은 화면을 처음 그릴 때(업로드·복원
   전, `PAY.inq.rows`가 빈 상태) 만들어지는데, v08.279 전에는 한 번 만들면 다시 만들지 않아
   업로드 뒤에도 계속 비어 있었다. 그래서 이 색인으로 사업자번호를 찾는 **유입경로 자동 판정만
   조용히 실패**했다(최종실적·가입채널은 행을 직접 받아 써서 정상 — "칸마다 값이 다르다"의 원인).
   플로우 원본 행을 fid로 찾는 코드를 새로 쓸 때는 이 색인이 최신인지 확인할 것.
9. **입력 칸에 id를 새로 붙일 때는 그 마크업에 이미 id가 있는지 먼저 확인한다** — 해지·재가입·
   정지업체 검색 칸(`#chQ`·`#spQ`)은 이미 id가 있었는데 못 보고 또 붙여 `id` 속성이 두 번 들어간
   적이 있다(v08.278 작업 중, 커밋 전에 되돌림). 죽은 화면(`inqShell`)의 `#inqQ`와 이름이 겹치는
   문제도 있었다 — `grep -n 'id="붙일이름"'`으로 먼저 확인할 것.

---

## 11. 주간회의 월별 표 2개 — 같은 기준·같은 열 폭

> **기준 설명은 표 제목 옆 (!) 버튼으로 연다**(v08.309 💡 → v08.310 (!) · 제목 옆으로 이동).
> · 버튼 = `wkNoteBtn(id)`(동그란 (!), 열려 있으면 청록으로 강조) — **제목 줄**(paintWeek)에 붙는다
> · 본문 = `wkNoteBody(id,html)` — 제목 바로 아래(표 위)에 접혀 있다
> · 문구 = `monPerfNoteHtml()` · `planJoinNoteHtml()` — **표 함수에서 분리**했으니 설명을 고칠 때는
>   이 두 함수만 열면 된다(표 함수 안에는 더 이상 설명이 없다)
> · 여닫기 = `window.wkNoteToggle(id,btn)`, 기본 접힘, 상태는 저장하지 않는다
> · **인쇄·PDF에서는 항상 펼쳐져 찍힌다** — 파일 상단 `@media print`의
>   `.wk-note{display:block!important}` / `.wk-note-btn{display:none!important}`

**두 표의 관계(v08.285)**: 「플러스 가입 및 프로모션 가입 — 월별 실적 현황」(`monPerfTableHtml`)의
「플러스 가입 실적」 행과 「전체 가입 현황」(`planJoinTableHtml`)의 「플러스」 행은 **같은 함수**
`planJoinCountRangeByIns(true, s, e)`를 쓴다 — 두 표의 그 값은 항상 같아야 한다(다르면 버그).
「└ 재가입」은 같은 기준의 재가입분만 세는 `planRejoinCountByIns(true, …)`다.
기준은 **인사이트 가입일자(`insJn`) 유무**(2026-09-06 확정 정책) — 요금제명(`pl`)이 아니다.
※ 「가입 목표」(`MONTARGET.new`)에 든 값은 **전체 가입 기준으로 입력된 옛 값**이므로 달성률이
낮게 보인다 — 사용자가 새 기준으로 다시 입력하기로 확정(2026-09-09).
※ 프로모션(개업성장패키지) 블록만 원천이 다르다(청구 파일의 프로모션 시작월, `pkgJoinCountRange`).

### 11-1. 열 정렬 (v08.283)

「플러스 가입 및 프로모션 가입 — 월별 실적 현황」(`monPerfTableHtml`)과 「전체 가입 현황」
(`planJoinTableHtml`)은 열 구성이 같다(구분 + 1~12월 + 합계 = 14열). 그런데 구분 칸의 글자 길이가
달라(「프로모션 가입 목표」 vs 「└ 개업성장패키지」) 브라우저가 표마다 다른 폭을 계산해, 위아래로
놓았을 때 같은 달 열이 서로 어긋나 보였다(예: 9월 열이 16px 차이).

- 두 표 모두 `<table class="wk-mtbl" style="font-size:10.5px;table-layout:fixed">`+`wkMonColgroup()`을 쓴다.
- `wkMonColgroup()`은 **구분 150px · 12개월 균등 · 합계 56px** colgroup을 돌려준다 — 열 폭이 내용과
  무관해지므로 두 표가 항상 같은 자리에 온다(화면·PDF 모두 확인).
- **`.wk-mtbl` 클래스 자체에는 손대지 않았다** — 같은 클래스를 쓰는 다른 표(주간 인사이트 등)까지
  고정폭이 되면 안 되기 때문. 그래서 클래스가 아니라 이 두 표의 인라인 style·colgroup으로만 적용한다.
- 열을 더하거나 뺄 때는 **`wkMonColgroup()`의 `<col>` 개수도 함께 맞춰야 한다**(안 맞으면 남는 열이
  0폭이 된다).

---

## 12. 「요금제 변경」을 세는 공통 규칙 (v08.286)

같은 「요금제 변경」인데 화면마다 기준이 달라 숫자가 4/5/6처럼 갈라졌던 문제를 규칙 하나로 모았다.

- **규칙 위치**: `window.planChgSkipWhy(x)` → `''`(실적) / `'재등록'` / `'구간 폐지'`,
  `window.planChgReal(x)`(불리언), `window.planChgSkipCount(rows)`(사유별 건수).
  **요금제 변경을 세는 코드는 반드시 이 함수를 쓴다** — 인라인으로 다시 조건을 쓰면 또 갈라진다.
- **제외 2종**(확정 정책 v01.21): ① 변경 전 = 변경 후(시스템 재등록) ② 구간요금제 → 베이직
  (폐지 일괄 전환). 변경 전/후 값이 **비어 있으면 제외하지 않는다**(없는 근거로 빼지 않는다).
- **플러스 해지(플러스 → 비플러스)는 제외 대상이 아니다** — 방향이 반대인 실적이다.
  주간 실적 요약만 따로 칸을 나눠 센다(요금제 변경 = 실적 기준 − 플러스 해지).
- **적용된 자리**: `perfStat`의 `plan`(월별·일별·누적 카드·발생 건 목록·실적 분석 채널표가 공유) ·
  `wkAutoPerf`(주간 실적 요약) · `wkEventRows`(주간 발생 건 목록) · `pmOrphanRows`(실적 관리
  「문의 없는 실적」).
- `perfStat`은 제외 전 전량을 `planAll`, 사유별 건수를 `planSkip`으로 함께 돌려준다 —
  월별·일별·누적의 요금제 변경 카드가 「실적 아닌 변경 N건 제외 (재등록 N · 구간 폐지 N)」로 보여준다.
- **활동 타임라인(`actRows`)은 그대로 전량**을 보여준다 — 여기는 실적이 아니라 "무슨 일이 있었나"의
  기록이라, 재등록도 활동이다(의도적 예외).

---

## 13. 「가입」 집계 공통 기준 (현재 = v08.314)

**세 화면이 같은 모수를 쓴다**(v08.303 — 사용자 확정: "주간회의내용과 월별 실적분석 데이터는
같아야돼"). `window.joinSplitCount(kind,s,e,onlyRejoin,scope)`의 `scope`는 **쓰지 않는다**(기본값).

| 화면 | 「플러스」 값 | 2026 합계(실데이터) |
|---|---|---|
| 「플러스 가입 및 프로모션 가입 — 월별 실적 현황」(`monPerfTableHtml`) | `joinSplitCount('plus',s,e)` | **83** |
| 「전체 가입 현황」(`planJoinTableHtml`) | 같은 함수·같은 인자 | **v08.346에서 행을 나눴다 — 아래 13-4 참고** |
| 실적 분석 카드(`perfStat`의 `plusInsN`) | 같은 함수·같은 인자 | 2026-08 = **20** (표 8월과 동일) |

**모수**(기본 scope) = 그 달의 신규가입 + 재가입 + 플러스 전환(요금제변경) + 인사이트 추가 가입.

- `scope='join'`(신규+재가입만)은 v08.300에서 「전체 가입 현황」에만 쓰다가 v08.303에서 **뺐다** —
  같은 이름의 「플러스」가 102 / 34로 갈려 두 화면을 나란히 놓으면 검산이 되지 않았다.
  함수의 `scope` 인자 자체는 남겨 두었지만 **지금 쓰는 자리는 없다**.
- 「리포트」는 모수를 넓혀도 값이 그대로다(58 = 58, 월별 값도 전부 동일) — 플러스 전환·인사이트
  추가로 들어오는 행은 모두 인사이트 보유라 리포트 쪽에 새로 붙는 행이 없다(실데이터 확인).
  즉 v08.303으로 바뀐 것은 「플러스」 행과 「합계」(92 → 160)뿐이다.

위 표는 실적이 **어디서 왔는지 3줄로 분해**해 보여준다(v08.301) —
`plus_new`(신규 플러스) · `plus_rejoin`(재가입) · `plus_upsell`(업셀: 인사이트 추가·플러스 전환).
우선순위는 신규 > 재가입 > 업셀이라 서로 겹치지 않고 **셋의 합 = 플러스 실적**이다
(2026 실데이터 26 + 1 + 56 = 83). 합계에 또 더하지 않는다.

**포함 관계는 표 위 「포함 비율 바」로 보여준다**(v08.307 — 사용자 확정) —
`monPerfTableHtml`의 두 달성률 카드 아래, 표 바로 위에 있는 즉시실행 함수가 그린다.
「플러스 가입 실적 N건 안에 개업세무사 프로모션 M건이 포함돼 있습니다 (x%)」 + 스택 바
(파랑 `#3B82F6` = 개업세무사 / 청록 `#7FD8D2` = 그 외 플러스). 실적이 0이면 바를 만들지 않고,
개업세무사가 플러스를 넘는 경우(프로모션 시작월 기준이라 이론상 가능)에는 **폭만 100%로 묶고
숫자는 실제 값**을 적는다.

**「└ 그중 개업세무사」 줄은 v08.306에서 제거됐다**(사용자 지시 "이거 빼") — 아래 파란 블록
(개업세무사 가입 목표 (플러스 내)·실적)과 값이 같아 같은 숫자가 두 번 나왔기 때문이다.
포함 관계는 **파란 블록 라벨의 「(플러스 내)」와 표 아래 문구**가 밝힌다. 값 계산(`rows.pkgA`
= `pkgStartCount`, 프로모션 시작월)은 파란 블록이 그대로 쓰므로 남아 있다.
※ 이 줄은 v08.302에서 「포함되는지 헷갈린다」는 지적으로 넣었다가 v08.306에서 뺐다 —
표 구성은 사용자 지시대로 두고, 내 판단으로 남기거나 되살리지 않는다.

### 13-2. 통합현황 「플러스 전환율」은 **요금제 기준**이다 (다른 화면과 모수가 다름 · 2026-09-10 확정)

통합현황(누적 요약)의 「플러스 전환율 (세모리포트 요금제 기준)」 카드는 **현재 정상 400곳 중
이용요금제가 플러스인 97곳**(24.2%)이다. 이 화면의 다른 「플러스」(주간회의 표·실적 분석 카드)는
**인사이트 보유** 기준이므로 숫자가 다르다 — **의도된 차이이고 사용자 확정으로 그대로 둔다.**

| 현재 상태 기준 | 곳 | 비율 |
|---|---|---|
| 이용요금제가 플러스 (이 카드) | **97** | 24.2% |
| 인사이트가 살아있는 곳(플러스 정의) | 101 | 25.2% |
| 인사이트 가입 이력 있음(해지·정지 포함) | 118 | 29.5% |

차이 4곳 = **요금제는 PREMIUM인데 인사이트를 쓰는 곳**(97곳은 101곳에 전부 포함된다).
※ 「2026 플러스 실적 중 인사이트가 남아 있는 건」과는 **다른 지표**다 — 그건 기간 실적의 잔존이고,
이 카드는 현재 상태(400곳)의 비중이다. 두 숫자를 섞지 않는다.

### 13-3. 플러스 실적 = **그 달에 인사이트를 새로 가입한 곳** (v08.314 · v08.353에서 재확정)

`joinSplitCount`의 판정은 두 개뿐이다.

| 함수 | 뜻 | 쓰는 곳 |
|---|---|---|
| `isPlus(x)` = `inR(x.insJn)` | **그 기간에 인사이트를 가입**했다 | plus · plus_new · plus_rejoin · plus_upsell |
| `hasIns(x)` = `insJn <= e` | 그 기간 종료일까지 인사이트를 보유한 상태 | `basic`(리포트) = `!hasIns` — **부르는 화면 없음** |

#### v08.351~v08.353 — 조건을 두 번 얹었다가 **원안으로 되돌린 기록** (다시 논의될 때 먼저 볼 것)

2026-09-23 하루에 사용자 지시가 셋 이어졌고, **셋을 모두 만족하는 판정은 v08.314 원안 하나뿐**이었다.

| 단계 | 사용자 지시 | 구현 | 2026 합계 | 무엇이 걸렸나 |
|---|---|---|---|---|
| v08.351 | "인사이트 가입일자가 있고 **현재 요금제가 플러스**인곳만" | `&& isPl(x.pl)` | 73 | `pl`은 **지금 시점 값** → 나중에 요금제를 내리면 **과거 달까지 줄어든다** |
| v08.352 | **"과거 숫자는 움직이면 안돼"** | `&& insAlive(x)`(그 기간 말까지 인사이트 유지) | 80 | 가입일·해지일만 보므로 과거는 고정 ○ |
| **v08.353** | **"가입 및 전환은 그 달 해지했더라도 가입 실적에 포함해야 함"** | **조건 전부 제거** | **86** | **가입은 가입이다** — 같은 달 해지도 그 달 실적 |

- **v08.351·v08.352는 배포되지 않았다**(그동안 main은 v08.349). 화면에 보인 값은 내내 86이다.
- 세 번의 왕복에서 남은 결론: **요금제를 기준으로 삼으려면 「요금제 변경 이력 로그」가 있어야 한다.**
  세무사무소목록의 `요금제 변경일시`·`변경 전 요금제`는 **마지막 1회분만** 남는 덮어쓰기 필드라,
  현재 플러스 105곳 중 **66곳은 원래 전환일이 이미 덮여 사라졌고**, 2026-07-03 하루에 34곳·
  06-30에 15곳이 **일괄 변경**으로 찍혀 있어 월별 실적이 그쪽으로 쏠린다(7월 6 → 50).
  개발팀 요청 컬럼: **이용기관아이디 · 변경일시 · 변경 전 요금제 · 변경 후 요금제 · 수정자명**
  (사무소명은 **동명 51곳**이라 조인 키로 쓰지 않는다).
- 「플러스 전환」 카드는 `offRows()`(전량)에서 요금제 변경일로만 세므로 **그 달 해지한 곳도 원래 포함**이다 — 손댈 것이 없었다.
- v08.353은 **2026-09-09 확정(아래 13-0)과 같은 결론**이다: 같은 달 가입·해지 건은 **포함 + 별도 표기**.

- **한 곳은 평생 한 번만** 플러스 실적으로 잡힌다(인사이트 가입일은 하나뿐이다) — 중복이 원천적으로 없다.
- **이미 플러스인 곳의 재가입·요금제 변경은 실적이 아니다**(2026 실데이터 14건).
  사용자 확정 근거: 「플러스에서 개업성장패키지 프로모션으로 요금제 변경하는 것도 결제 금액·수단의
  차이일 뿐이고, 개업세무사 프로모션으로 따로 카운트도 하고 있으므로 그런 요금제 변경과 단순 중복인
  경우는 제외하는 게 맞음」(2026-09-10). 개업세무사는 파란 블록에서 따로 관리한다.
- 2026 실데이터(v08.314 당시): **플러스 83**(12·13·10·11·4·6·6·19·2) · 세부 3줄 **26 · 1 · 56** ·
  리포트 63 · 「전체 가입 현황」 합계(플러스+리포트) **146**.
  **전체 가입 모수 160 = 플러스 83 + 리포트 63 + 이미 플러스인 곳의 활동 14** — 그래서 합계가
  모수보다 작다(표 아래 문구가 밝힌다).
- 여기까지 온 경로(모두 이 규칙에 흡수됐다): v08.312 「인사이트 가입일이 그 달 이전인 곳만」(시점
  조건, 102→97) → v08.313 「그 해 안 중복 제거」(97→89) → **v08.314 「인사이트 가입월로 못 박기」(89→83)**.
  ①안(그 해 안에서만 중복 제거)은 연도를 넘는 중복이 남아 채택하지 않았다.

### 13-0. 「당월 인사이트 해지」 지표는 화면에서 전부 뺐다 (v08.305 → v08.309·v08.311)

인사이트를 가입한 그 달에 곧바로 해지한 건이 있다(2026-08 실데이터 5곳 — 해지사유가 전부
「베이직 요금제 변경으로 인한 해지」다. 8/11 가입 → 8/20 해지 식). 사용자 확정은 **포함 + 별도
표기**: 가입일 기준으로 세는 원칙(2026-09-09)은 그대로 두고, 몇 건이 곧 떨어졌는지만 함께 보여준다.

- **지금은 지표로 표시하지 않는다** — 주간회의 표 행은 v08.309에서, 실적 분석 카드 밑줄은
  v08.311에서 사용자 지시로 뺐고, 계산 함수(`joinSplitCount('plus_insout')`)도 부르는 곳이 없어
  함께 지웠다(죽은 코드를 남기지 않는다). 인사이트 해지는 **발생 건 목록의 「인사이트 해지」 행**에서
  원건으로 확인한다(2026-08 = 8건 · 그중 당월 가입분 5건).
- 필요해지면 되살리는 계산은 간단하다: 모수(`rows`) 중 `insJn`이 있고 `insCn`이 그 기간인 곳.
- **실적에서 빼지 않는다**(가입일 기준 원칙 유지) — 그 판단은 그대로이고, 표시만 없앤 것이다.
  ※ 표 구성·밑줄 문구는 사용자 지시대로 두고 내 판단으로 되살리지 않는다.
- 파서: `insSt`(인사이트 상태) · `insCn`(인사이트 해지일) · `insRs`(인사이트 해지사유) —
  인식 여부는 `__xlOfficeCols.insSt/insCn/insRs`.

### 13-1. 카드 밑줄도 표의 3줄과 같은 숫자를 쓴다(v08.303)

실적 분석 카드(`perfStat` → `plusNote`)의 밑줄은 v08.302까지 **모수의 경로별 원건수**
(신규 9 · 재가입 1 · 플러스 전환 13 · 인사이트 신규 19)를 적어, 주간회의 표의 세부 3줄
(신규 플러스 2 · 재가입 0 · 업셀 17)과 숫자가 달라 어긋난 것처럼 읽혔다
(2026-09-09 질문 "데이터가 다른이유"). v08.303부터 밑줄도 **같은 함수**
(`joinSplitCount('plus_new'|'plus_rejoin'|'plus_upsell')`)로 3줄을 그대로 적고, 모수는
「가입 후보 27건 중」으로 맥락만 남긴다.

옛 밑줄(모수 내역)과 3줄이 왜 달랐는지는 아래와 같다 — 같은 질문이 다시 나오면 이 설명을 쓴다
(2026-08 실데이터).

| 숫자 | 뜻 |
|---|---|
| 모수 27건(신규 9 · 재가입 1 · 플러스 전환 13 · 인사이트 신규 19) | 경로별 원건수 — 인사이트 유무를 보지 않고, 한 곳이 여러 경로에 겹쳐 잡힌다(합 42 → 중복 제거 27) |
| 플러스 19건 | 모수 27곳 중 **8월에 인사이트를 새로 가입한** 곳(v08.314) |
| 3줄(2 · 0 · 17) | **19건**의 상호배타 분해(우선순위 신규>재가입>업셀) |

- 「신규 9 vs └ 신규 플러스 2」 = 8월 신규가입 9곳 중 인사이트까지 가입한 곳은 2곳
  (우주 세무회계·리유세무회계), 나머지 7곳은 인사이트 없음(리포트).
- 8월 재가입 1건(세무그룹 서울택스, 인사이트 5월)은 **이미 플러스인 곳의 활동**이라 8월 실적에서
  빠진다(v08.314) — 그래서 8월 플러스는 **19**, 세부 3줄은 2 · **0** · 17이다.
- 「플러스 전환 13 vs └ 업셀 17」 = 업셀 17 = 플러스 전환 12 + 인사이트만 새로 붙인 5곳.
  플러스 전환 13건 중 1곳(리유세무회계 08-28 가입 → 08-31 플러스)은 **신규**로 이미 세어
  업셀에서 빠진다(우선순위). 인사이트만 붙인 5곳은 요금제명이 베이직이어도 플러스로 센다
  (v08.296 확정 정책 — 인사이트 가입 자체가 실적).

두 표 아래에 각각 기준 설명 문구가 있다 — 지금은 「세 화면의 플러스 숫자는 항상 일치한다」를
밝히는 문구다(v08.303).
`└ 개업세무사`는 두 표 모두 **프로모션 시작월** 기준(`pkgStartCount`)이라 모수와 무관하다.
**그래서 월별로는 개업세무사가 그 달 플러스보다 클 수 있다**(2026-09-16 파일: 9월 플러스 3 vs
개업세무사 4 — 현솔·리유세무회계가 인사이트 08-31·프로모션 09월). **2026-09-16 사용자 확정으로
현행 유지 — 정합 오류로 보지 말 것.** 연간으로는 모순이 없어(전원 인사이트 가입이 2026년)
「(플러스 내)」 표기와 포함 비율 바는 **연간 기준으로 읽는다**
(사례는 `사업센터_데이터정합규칙_*.md`의 「의도된 차이」 절).


계산은 `window.joinSplitCount(kind,s,e,onlyRejoin)` 한 함수만 쓴다(`kind` = `'total'|'plus'|'basic'|'pkg'`).

| 항목 | 판정 | 기간 기준 |
|---|---|---|
| **가입(모수)** | 그 기간에 **가입일 · 재가입일 · 요금제변경일 · 인사이트 가입일자** 중 하나가 찍힌 사무소(행 단위 1건). 요금제변경은 **실적으로 보는 변경**(`window.planChgReal`, 12번 섹션) 중 **플러스로 올라온 건만**(v08.295). **인사이트 신규 가입은 그 자체가 실적**(v08.296 — 그 곳은 항상 플러스로 분류) | 고객 마스터 |
| **플러스** | 모수 중 **인사이트 가입일자(`insJn`)가 있는** 곳 — **개업세무사 포함**(빼지 않는다) · **그 달에 해지했어도 포함**(v08.353 확정) | 고객 마스터 |
| **베이직** | 모수 중 `insJn`이 없는 곳 | 고객 마스터 |
| **합계** | **플러스 + 베이직** (= 모수 전체) | 고객 마스터 |
| **개업세무사** | **프로모션 시작월이 그 달**이고 프로모션 구분명이 개업세무사 성장지원 패키지인 곳 → `window.pkgStartCount(s,e)` | **프로모션 시작월**(가입 모수와 별개) |

- **행 순서·표기는 사용자 스펙 그대로다**(v08.298) — 「전체 가입 현황」은 **플러스 / └ 개업세무사 /
  리포트 / 합계** 4행이다. 개업세무사는 프로모션 시작월 기준이라 날짜 기준이 다르지만, **하위(└)
  표기는 스펙이므로 유지**하고 「합계에 더하지 않음」은 표 아래 문구로 밝힌다
  (v08.294에서 별도 행으로 내린 것은 내 판단이었고 되돌렸다).
- **표 구성은 사용자 스펙 그대로다** — 「월별 실적 현황」은 **플러스 가입 목표 / 플러스 가입 실적 /
  └ 신규 플러스 / └ 재가입 / └ 업셀(인사이트·전환) / 달성 실적 / 달성률 + 개업세무사 4행**(v08.309 기준). `└` 줄은 모두 **플러스 가입 실적 안의 내역**이라
  합계에 또 더하지 않는다(`└ 재가입` = `joinSplitCount('plus',s,e,true)`).
  ※ `└ 재가입`은 v08.297에서 「스펙에 없다」는 지적으로 지웠다가 v08.299에서 사용자가 다시 넣었다 —
  스펙이 바뀌면 그 스펙을 따르고, 임의로 남기거나 지우지 않는다.
- **상태(정상·정지·해지)는 보지 않는다** — 해지는 별도 지표로 세므로, 지금 해지된 곳도 **가입일이
  그 달이면 그 달 가입으로 센다**(2026-09-09 사용자 확정, 실데이터로 확인: 2026-01 가입 20곳 중
  5곳이 현재 해지 상태이며 그대로 포함). 유일한 예외는 「프로모션 종료 관리 목록」으로,
  거기서만 **현재 상태가 정상인 곳**만 남긴다(안내 대상이 아니므로 — 14번 섹션.
  v08.326 전에는 해지일·정지일 유무로 걸러 지금 정상인 곳까지 빠졌다).
- **방향(v08.295)**: 요금제변경은 플러스로 **올라온** 건만 가입으로 본다 — 방향을 보지 않으면
  플러스에서 내려간 곳까지 「플러스 가입」에 잡힌다(실데이터 2026-08: 플러스 23 → 15).
- **판정 원천(v08.290)**: 세무사무소목록(마스터)에 **「프로모션 구분명」 컬럼이 있으면 그 값만** 본다
  (`window.pkgHasMaster()`). 그 컬럼이 없는 예전 파일이면 청구대상 고객관리 파일(promo)로 되돌아간다.
  마스터 파서는 CS·CT·CU 열을 `pmNm`(구분명) · `pmS`(시작 연월 YYYYMM) · `pmE`(종료 연월)로 담고,
  인식 여부를 `__xlOfficeCols.pmNm/pmS/pmE`에 남긴다. 값이 `-`인 행은 빈 값(프로모션 없음)이다.
- **개업세무사는 별도 행**이다(v08.294) — 프로모션 구분명에는 시점 정보가 없어 「가입 모수 중
  구분명이 개업세무사인 곳」으로 세면 **1월에 가입한 뒤 7월에 프로모션을 붙인 곳이 1월로 잡힌다**
  (프로모션이 없던 달에 숫자가 생기던 원인). 그래서 **프로모션 시작월**로 센다:
  `window.pkgStartCount(s,e)` — 마스터의 `pmNm`+`pmS`가 1순위, 없으면 청구 파일(`pkgJoinCountRange`),
  둘 다 없으면 `null`. 상태(정상·정지·해지)는 보지 않는다(그 달에 시작된 사실이 실적).
  가입일 기준인 플러스·리포트와 날짜 기준이 달라 **하위(└)로 붙이지 않고 합계에도 더하지 않는다**.
- 프로모션 정보가 아무것도 없으면(마스터 컬럼도, 청구 파일도 없음) 개업세무사는 **null(화면 「—」)**.
- 이 규칙은 v08.288(세 갈래 상호배타)·v08.289(프로모션 시작월 기준)를 차례로 대체한 **최종본**이다.
- **행 이름(v08.290)**: 인사이트 미가입 행은 **「리포트」** — 이 행에는 베이직뿐 아니라 PREMIUM·
  STANDARD·구간·기본 같은 과거 요금제가 모두 들어오므로 특정 요금제 이름(베이직)은 오해를 부른다.
- **적용된 자리(모두 같은 함수)**: 주간회의 「플러스 가입 실적」·「└ 재가입」·「개업세무사 가입 실적」
  (`monPerfTableHtml`) / 「전체 가입 현황」 4행(`planJoinTableHtml`) / 실적 분석 카드
  (`perfStat`의 `plusInsN`·`pkgJoinN`) / 주간 실적 요약(`wkAutoPerf`).
- **이름**: 「프로모션 가입」 → **개업세무사 가입**, 「리포트」 → **베이직**(v08.240의 리포트 개명을
  이번 스펙으로 되돌림). 주간 실적 요약 KPI는 **저장 키(`'개업성장패키지 가입'`)를 그대로 두고**
  `WKKPI_LABEL`로 화면 이름만 바꾼다 — 키를 바꾸면 직접 입력해 둔 값이 사라진다.

### 13-1. 옛 기준(참고, v08.287)

세 화면이 같은 이름으로 서로 다른 값을 보여주던 것을 하나로 통일했다(2026-08: 카드 1건 vs 표 3건).

- **기준**: 고객 마스터의 **인사이트 가입일자(`insJn`) 유무** · 모수는 **신규 가입 + 재가입**
  (2026-09-06 확정 정책). 요금제명(`pl`/`bf`)은 **쓰지 않는다**.
- **계산 함수 하나**: `planJoinCountRangeByIns(true, s, e)` — 아래 세 자리가 이 함수를 호출한다.
  | 화면 | 값 |
  |---|---|
  | 실적 분석 월별·일별·누적 「플러스 가입」 카드 | `perfStat`의 `plusInsN` |
  | 주간 실적 요약 「플러스 가입」 | `wkAutoPerf`의 `'플러스 가입'` |
  | 주간회의 「플러스 가입 실적」 행 | `monPerfTableHtml`의 `rows.newA` |
- **「플러스 전환」은 별개 지표**로 요금제명 기준 그대로다(`plusConv` — 변경 전이 플러스가 아닌
  요금제 변경 건). 인사이트 가입일자로는 "요금제를 올렸다"를 알 수 없다.
- 옛 기준 값(`plusNew`/`plusJoinN` = 신규 가입 중 가입 시점 요금제가 플러스+패키지)은 **계산만
  남겨 뒀다** — 화면에서는 쓰지 않는다. 되살릴 일이 없으면 지워도 된다.
- **주의**: v08.182에 "월 목표가 신규 기준이라 카드도 신규만 센다"는 근거가 남아 있는데, v08.287
  결정으로 무효다. 월 목표 카드(「신규 가입 + 플러스 전환」, 정책 v01.34)는 그대로이고, 카드
  밑줄에 기준을 적어 두 값이 다름을 드러낸다.
- 「전체 가입 현황」 표의 **개업성장패키지는 별도 행**이다(v08.287) — 청구 파일의 프로모션
  시작월 기준이라 가입일 기준인 플러스·리포트·합계와 **기간 귀속이 달라 포함 관계가 아니다**
  (2026-08 실제로 플러스 3 < 패키지 5). 하위(`└`) 표기로 되돌리지 말 것.

- **개업세무사 가입 목표 입력칸은 12개월 모두 열려 있다**(v08.293) — v08.257의 「2026-07 이전
  비활성화」(`PKG_PROMO_START_YM`)는 프로모션 시작월로 실적을 세던 시절의 규칙이라 함께 지웠다.
  가입일 기준으로 세면 프로모션 시작 전 달에도 실적이 잡힐 수 있어 목표칸을 막으면 달성률을 못 낸다.

---

## 14. 프로모션 종료 관리의 원천 (v08.291 · 모수 수정 v08.326)

「프로모션 종료·정상가 전환」 화면(`#s-promo`)의 두 표와 통합현황 「3개월 내 종료」 카드는
**세무사무소목록(마스터)** 을 우선 원천으로 쓴다 — 청구대상 고객관리 파일이 없어도 동작한다.

- **원천 선택**: `window.promoRowsMaster()` — 마스터에 「프로모션 구분명」 컬럼이 있으면
  (`__xlOfficeCols.pmNm`) 그 파일의 행을 돌려주고, 없으면 `null`(그때는 종전 청구 파일 로직).
  모수는 **현재 상태가 「정상」인 행**이다(`String(x.st).trim()==='정상'` — 이 파일의 다른 화면과 같은 규칙).
  상태 값이 아예 없는 예전 파일이면 종전 규칙(해지일·정지일이 비어 있는 행)으로 돌아간다.
  - **v08.326에서 고친 버그**: 종전 모수는 「해지일·정지일 칸이 **비어 있는** 곳」이었다.
    그런데 마스터에는 **정지했다가 해제한 곳·해지했다가 재가입한 곳**에도 그 날짜가 남아 있어,
    **지금 정상으로 이용 중인데도** 표에서 사라졌다 — 2026-09-16 파일 기준 개업세무사 15곳 중
    화면에는 **8곳**만 나왔고, 빠진 7곳 중 6곳이 현재 상태 「정상」이었다(수정 후 **14곳**,
    해지 상태인 세무회계 세그루 1곳만 제외).
  - 교훈: **「날짜 칸이 비었는가」로 현재 상태를 판단하지 않는다** — 그 컬럼은 이력이지 상태가 아니다.
- **종료 관리 목록**(`window.__syncPromo`): 프로모션명·종료월은 마스터(`pmNm`·`pmE`),
  **면제·할인은 마스터에 없는 정보**라 청구 파일이 함께 있으면 그것만 보조로 붙인다.
  한 줄을 만드는 판정(무기한/종료 경과/3개월 전 대상/진행 중 + `need1m`)은 원천과 무관하게
  내부 `push()` 하나에서만 한다 — 규칙을 두 번 쓰지 않는다.
  어느 원천을 썼는지는 `A['프로모션원천']`(`'마스터'`|`'청구'`)에 남는다.
  - **호출 지점은 3곳이다**(v08.326에서 하나 추가): ① 페이지 로드 직후 ② 청구대상 고객관리
    업로드 뒤 ③ **세무사무소목록 업로드 뒤**(`markUpload('office')` 바로 아래).
    ③이 빠져 있어, 1순위 원천인 세무사무소목록만 올리면 이 목록과 통합현황 「3개월 내 종료」가
    **새로고침 전까지 옛 값 그대로**였다(실측: 216건인데 화면 0건).
    → **원천을 새로 붙일 때는 그 파일의 업로드 분기에도 sync 호출을 넣는다.**
- **190만(개업세무사) 일시납 스케줄**: `pkg190Rows`가 마스터 행을 `{nm, promo:pmNm,
  promoS:pmS, promoE:pmE}`로 맞춰 종전 판정·표시 로직을 그대로 재사용한다.
- 기준월(`base`)은 마스터 기준일 때 `__xlOffice.maxDate`(없으면 `__baseDate`, 그다음 청구
  `baseYm`)를 쓴다. `lim`=+3개월, `lim1`=+1개월은 종전과 같다.

---

## 15. 새 버전 알림 띠 (v08.292 · 시작 지점 v08.323 · 감지 방법 v08.328)

배포는 화면 파일만 바꾸고 **이미 열려 있는 창은 자동으로 새로고침되지 않는다**(이 파일에서 강제
새로고침은 로그아웃 한 곳뿐). 그래서 새 배포를 감지해 **알리기만** 한다 — 강제 새로고침은 하지 않는다
(입력 중인 내용이 날아가면 안 된다).

- `window.verWatchStart()` — **로그인 경로 2곳 모두**에서 호출한다(둘 다 `updateSaveBadge()` 뒤).
  ① `init()` — 페이지를 열 때 이미 로그인돼 있던 경우
  ② `sb.auth.onAuthStateChange`의 로그인 분기 — **로그인 화면을 거쳐 들어온 경우**
  `badHost()`가 비어 있지 않으면(파일 직접 열기·미리보기·iframe) **시작하지 않는다**.
  중복 호출은 `__verWatch.on`이 막으므로 `setInterval`은 한 번만 걸린다(검증 완료).
  ※ **v08.323에서 고친 버그**: ②가 빠져 있어, 로그인 화면을 거쳐 들어온 창은 배포가 나도
    끝까지 알림 띠를 받지 못했다(사용자 신고 "원래 윗상단에 알림이 왔었는데 지금 안와").
    감시를 시작하는 자리를 새로 만들 때는 **로그인 경로가 둘이라는 것**을 먼저 확인한다.
- 감지(2단): **2분마다 자기 페이지에 HEAD 요청**(v08.334) → `ETag`/`Last-Modified`/**`Content-Length`**가
  처음 값과 달라지면 새 배포. 본문(1.4MB)은 받지 않는다.
  · v08.334부터 **안 보이는 탭도 건너뛰지 않는다**(사용자 요청 "무조건 배포시 배포알림이 올수 있도록").
  · `Content-Length`를 태그에 더한 이유 — ETag를 안 주는 서버·CDN도 길이는 거의 항상 준다.
  새 배포가 확인된 뒤에만 `Range: bytes=0-262143`으로 앞부분을 받아 `__APP_VER`를 읽는다
  (그 상수는 파일 시작에서 **약 164KB** 지점 — Range 미지원이면 전체를 받아 같은 정규식으로 찾는다).
- **폴백(v08.328) + 정기 확인(v08.334)**: 헤더가 **셋 다 없거나** HEAD가 실패하면 `verByBody()` —
  버전 문자열을 직접 읽어 지금 이 창의 `window.__APP_VER`와 비교한다. 헤더에 기대지 않는 가장 확실한 방법.
  v08.334부터는 **마지막 본문 확인에서 5분이 지나면 헤더가 그대로여도 `verByBody()`를 한 번 돌린다**
  (`__verWatch.lastBody` 기준 — 호출 횟수가 아니라 **시간**으로 센다. `visibilitychange` 같은 추가
  호출 때문에 주기가 들쭉날쭉해지지 않게 하려는 것). 파일 크기까지 똑같은 배포도 이 단계에서 잡힌다.
- **v08.328에서 고친 버그**: 종전 `tagOf()`는 헤더가 하나도 없어도 `'|'`를 돌려줬다. 그 값이
  **참**이라 「헤더 없으면 판단 불가」로 빠지는 `if(!tag)`가 한 번도 걸리지 않았고, base가 늘 `'|'`
  → 이후에도 늘 `'|'` → **영원히 「변경 없음」**이었다. 헤더를 안 주는 서버·CDN에서는 알림이
  구조적으로 뜰 수 없었다(재현 검증 완료: 헤더 없는 서버에서 수정 전 `base="|"`, 배포해도 띠 없음).
- **v08.328 추가**: `visibilitychange`로 **탭이 다시 보이는 순간 한 번 더 확인**한다 —
  자리를 비운 사이에 나간 배포를 5분 더 기다리지 않는다.
- 표시: `window.verBar(newVer, tag)` → `#verNewBar`(고정 상단 띠, z-index 10003).
  [새로고침] = `location.reload()`, [나중에] = 그 배포(tag)만 다시 알리지 않음(`__verWatch.dismissed`).
- 상태: `window.__verWatch = {base, dismissed, on, ms, check}` — `ms`로 주기를 바꿀 수 있고
  (기본 5분) `check()`를 직접 불러 한 번만 확인할 수도 있다(점검·테스트용).
- 띠가 세 개 겹칠 수 있다 — `#hostWarnBar`(10002) · `#saveFailBar`(10001) · `#verNewBar`(10003).
  새 띠를 추가할 때 z-index와 `top` 겹침을 함께 확인한다.

## 16. 「해지」를 세는 기준 (v08.305)

**기간 집계는 해지일 기준 전량**이다 — 현재 상태로 걸르지 않는다(사용자 확정 2026-09-10).

| 자리 | 기준 | 함수·값 |
|---|---|---|
| 일별·월별 카드·발생 건 목록 | **해지일이 그 기간** | `perfStat().churn` |
| 주간회의 ① 실적 요약·①-3 목록 | 같음 | `wkAutoPerf`의 `_cn` · `wkEventRows` |
| 일별 해지 목록 | 같음 | `window.dayChurnRows(d)` |
| **누적(전체 기간) 카드·목록** | **예외 — 지금 해지 상태인 곳** | `perfStat().churnNow`(누적 렌더러가 `S.churn`에 덮어씀) · `window.allChurnRows()` |

- 왜 바꿨나: 종전에는 「해지일 + 현재 상태가 해지」로 세어 **해지 후 다시 돌아온 곳이 빠졌다**.
  회사 시스템의 월별 현황 시트(2026-09-10 검증 파일 「현황」 탭)와 대조하니 차이가 정확히 4건이었다 —
  1월 13/14 · 4월 17/18 · 5월 5/6 · 8월 8/9(최원석 세무사·세무회계 모아·세무그룹 서울택스·이원희 세무회계,
  모두 해지 후 재가입). 가입을 「지금 해지됐어도 가입일 기준」으로 세는 원칙과 대칭이 됐다.
- **누적만 예외인 이유**: 거기서 「누적 해지」는 「지금 해지 상태인 곳」이고,
  「누적가입 − 누적해지 − 누적정지 = 현재 정상」 검산(v08.98 사용자 확정)이 그 정의에 기댄다.
  누적 목록(`allChurnRows`)도 같은 기준이라 카드와 목록이 어긋나지 않는다.
- 검증(2026): 월별 해지 14·19·12·18·6·13·16·9·2 = 시스템 현황 시트와 **전량 일치**.
  가입·재가입·정지·정지해제도 9개월 전부 일치(81·11·89·64).

## 17. 발생 건 목록의 구분 9종 (v08.305)

`eventRows(S)` — 문의 · 요금제변경 · 가입 · 재가입 · **인사이트 가입** · **인사이트 해지** ·
정지 · 정지해제 · 해지. 정렬 키는 `TAG_ORDER`(그 순서 그대로).

- 인사이트 2종을 넣은 이유: 「플러스 가입」·「업셀」의 근거가 인사이트 가입일인데 목록에 그 행이
  없어 화면에서 검산이 안 됐다(2026-09-09 질문 "플러스 업셀이 17개가 맞아?").
  2026-08 = 78건(요금제변경 22 · 인사이트 가입 19 · 가입 9 · 해지 9 · 인사이트 해지 8 · 정지 5 ·
  정지해제 5 · 재가입 1). 업셀 17 = 인사이트 가입 19 − 신규가입 2로 목록에서 바로 확인된다.
- 「인사이트 해지」 행의 사유 칸은 `insRs`(없으면 「미기재」).

## 18. 주간 「개업세무사 가입」은 월 기준이다 (v08.305)

개업세무사는 **프로모션 시작 연월(YYYYMM)** 로만 판정할 수 있어 주 단위로 쪼갤 수 없다.
`wkAutoPerf`는 **주 종료일이 속한 달** 한 달을 센다(주간회의 귀속 규칙과 동일) —
라벨도 `WKKPI_LABEL`에서 「개업세무사 가입**(월)**」로 표시한다.

- 고친 버그: 종전에는 주 시작~종료를 그대로 넘겨, 주가 두 달에 걸치면 **두 달이 합산**됐다
  (2026-08-27~09-02 주 = 8월 5 + 9월 2 = **7건**으로 표시 → 지금은 9월 기준 2건).
- 그래서 이 칸은 「그 주에 시작된 건수」가 아니다 — 2026-09는 세무회계 채담(9/4 신규가입)과
  세무회계 현솔(2024년 가입 고객 · 인사이트 8/31 · 프로모션 9월 시작) 2곳이며, 현솔은 그 주
  발생 건 목록에 나타나지 않는다.

## 19. 업로드 스냅샷 — 인사이트 사용건수(v08.308) + 통합현황 증감(v08.327)

마스터 파일의 인사이트 사용건수 3종(총·자금일보·전자세금계산서)은 **현재 누적값**이라 파일
하나로는 추이를 만들 수 없다. 그래서 **업로드할 때마다 그 날의 값을 스냅샷으로 쌓아** 그린다.

- 적립: `xlParseOffice` 안(office_rows 저장 직후)의 즉시실행 블록.
  `window.__insSnap` = `[{d, bd, base, all, allU, fund, fundU, tax, taxU}, …]`
  · `d` = **업로드한 날**(TODAY) — 하루에 여러 번 올리면 덮어쓴다(점은 하루 하나).
    파일 기준일로 키를 잡으면 그 날짜가 며칠 그대로일 때 점이 늘지 않아 이렇게 정했다.
  · `bd` = 파일 안의 가장 늦은 날짜(참고용). **오늘 이후 날짜는 제외**한다 — 인사이트 해지일처럼
    미래 날짜가 파일에 들어 있다(2026-09-12 등).
  · 컬럼이 없는 항목은 `null`(0으로 지어내지 않는다). 최근 **36개**만 보관.
- 저장: `STATE_MAP`의 `ins_snap` → `IX_saveToSheet('ins_snap',…)`, 복원은 `IX_loadFromSupabase`.
- 표시(월별): `_insMonthlyHTML()` — 3카드 아래 **「월별 사용건수」 표**(v08.320). 달별 마지막 스냅샷의 누적값 차이를 그 달 증가분으로 본다(첫 달은 비교 대상이 없어 넣지 않는다). 업로드를 거른 달이 있으면 그 구간이 합쳐지므로 비교한 두 기준일을 함께 적는다. 스냅샷이 1회뿐이면 표 대신 안내 문구.
- 표시(추이): 통합현황 「인사이트 사용 현황」 3카드 안의 `_insSpark(key)` — SVG polyline + 마지막 점 강조,
  아래에 「▲ +34건 (+2.7%) 지난 업로드 2026-09-08 대비 · 스냅샷 4회(…~…)」.
  **스냅샷이 1개면 선을 그리지 않고** 「다른 날 파일을 한 번 더 올리면 그려집니다」로 둔다.

## 20. 「실적」 = 가입 + 요금제 상향 (사건 기준 · v08.322)

사용자 확정(2026-09-16): **실적 = 신규가입 + 재가입 + 요금제 상향**, **가입 = 신규가입 + 재가입**.
그리고 **「요금제 상향은 실적이지만 전체 가입이 늘어난 것은 아니다」** — 그래서 가입을 세는 표에는
상향을 더하지 않는다.

- 공통 함수: `window.perfEvents(s,e)` → `[{row, kind:'신규가입'|'재가입'|'요금제상향', date}]`
  · 상향 판정은 `window.perfIsUpgrade(x)`(= `planChgReal` 통과 + 변경 전 비플러스 → 변경 후 플러스)
    — `joinSplitCount`(v08.295)와 **같은 규칙**을 함수 하나로 모았다.
  · **사건 단위**라 한 곳이 같은 기간에 가입도 하고 상향도 하면 **2건**이다(사용자 확정).
    `joinSplitCount`는 **사무소 수**(행 하나 = 1건)라 세는 단위가 다르다 — 두 값은 같지 않아도 된다.
  · 2026 실데이터: 신규가입 84 · 재가입 11 · 요금제 상향 31 = **126건**
    (월별 계 20·25·12·10·8·6·13·22·10)

**쓰는 곳** (v08.324 — 실적을 한 표에 섞지 않고 **표를 나눠** 보여준다)
| 화면 | 모수 | 비고 |
|---|---|---|
| 실적 분석 「가입채널별 실적 (가입+재가입)」 `inflowBlockHTML`의 `t2` | 가입만(`joinEv`) | 채널 미지정 건 제외 |
| 실적 분석 「최종실적별 실적 (가입+상향)」 `t3` | **가입 + 상향**(`perfEv`) | v08.329 — 최종실적은 「어느 채널의 성과로 귀속되는가」라 실적 전체가 들어간다 |

**표 배치**(v08.332 사용자 요청 "자리 바꿔"): 1행 `t1`(유입경로) · `t2`(가입채널) / 2행 `t3`(최종실적) · `t4`(상향).
| 실적 분석 「요금제 상향 채널별 실적 (상향만)」 `t4` | **상향만**(`upEv` = `perfIsUpgrade`) | 소분류는 「변경 전 → 변경 후」 |
| 「전체 가입 현황」 `planJoinTableHtml` | **바꾸지 않았다** | 인사이트 신규 가입월 기준(플러스/리포트) |
| 주간회의 표 · 주간 실적 요약 | **바꾸지 않았다** | `joinSplitCount` 그대로 |

**t2 + t4 = t3 = 실적 전체**다(v08.329 — 사용자 확정). 하향·기타 요금제 변경은 이 화면에서 빠지고 **세부 지표 「요금제 변경」**
카드(`S.plan`, 제외 사유 포함)에서만 본다.
※ v08.322에서는 t2·t3의 모수를 「가입 + 상향」으로 넓혔었다 — v08.324에서 **표를 나누는 방식으로
바꿨다**(사용자 지시). 「실적 = 가입 + 상향」이라는 정의(`perfEvents`)는 그대로다.

**차이 메모(사용자 요청 "왜 차이가 나는지 알수있게 메모")**
- 실적 분석: 2열 그리드 **바깥**(`inflowBlockHTML` 반환값 끝)에 붙는 `_memo` —
  「실적 N건 = 가입 N건(신규 N · 재가입 N) + 요금제 상향 N건」 + 「전체 가입 현황과 세는 단위가
  다르다」 + 「채널 지정된 건만 센다」 3줄. 그리드 안에 넣으면 2열이 깨진다(v08.173에서 겪음).
- 「전체 가입 현황」: `planJoinNoteHtml()`에 「요금제 상향은 가입 수를 늘리지 않는다」 문구 추가.

## 21. 「전체 문의」와 「도입 문의」는 다르다 (v08.325)

사용자 지적(2026-09-16): 실적 분석 핵심 카드의 **「도입 문의」는 사실 전체 문의**였다
(v07.16에서 신규가입·재가입·요금제변경 유형까지 포함하도록 넓힌 뒤 이름만 그대로였다).

| 이름 | 값 | 판정 |
|---|---|---|
**결론(v08.333 사용자 확정 "신청이 도입문의가 맞아" + 「16건 · 신청은 모두 도입문의」)**:
고객이 낸 신청은 유형 이름과 무관하게 **모두 도입문의**다. 그래서

| 화면 | 이름 | 값 | 판정 |
|---|---|---|---|
| 실적 분석(일별·월별·누적) | **전체 문의** | `perfStat`의 `S.inq` | `isPerfTarget` — 테스트·공지·하위업무만 제외 |
| 주간 실적 요약 | **도입문의** | `wkAutoPerf`의 같은 판정 | 같음 |

**두 값은 항상 같다** — 이름만 다르다(사용자가 각 화면에서 쓰던 말을 그대로 뒀다).

- `perfStat`의 **`S.inqByCls`** = `{inq, join, plan}` 세 갈래 건수(합 = 전체 문의).
  「전체 문의」 카드 밑줄에 **「일반 문의 6 · 가입 신청 8 · 요금제 신청 2」** 로 적는다.
  라벨에 「도입」을 쓰지 않는 이유 — 「도입문의」가 이제 **전체**를 가리키는 말이라, 같은 단어를
  두 뜻으로 쓰면 다시 헷갈린다.
- ※ v08.325에서 「전체 문의(16)」·「도입 문의(6)」 두 카드로 나눴다가 **v08.333에서 하나로 되돌렸다**
  (사용자: "여기서도 도입문의 빼자. 전체문의에 설명이 있으니까 굳이 필요없어"). `S.inqPure`와
  `.kpi-head.five` CSS는 쓰는 곳이 없어져 **함께 지웠다**(죽은 코드를 남기지 않는다).
- `inqCls`는 **문의 유형 이름**만 본다(`/신규가입|재가입/`→join, `/요금변경|요금제/`→plan, 나머지 inq).
  이 분류는 **밑줄의 세 갈래를 나누는 데만** 쓰이고, 도입문의 총계에는 영향이 없다.

- **전체 문의 = 도입 문의 + 가입 유형 + 요금제 유형**.
- 카드: `cards6`의 `hero('전체 문의', …)` · `hero('도입 문의', …)` — 핵심 카드가 5개가 되어
  그리드 클래스가 `kpi-head four` → **`kpi-head five`**(CSS도 `.kpi-head.five`로 신설).
- 월 목표 카드(`goalBlockHtml`)의 라벨도 「전체 문의」로 맞췄다 — 세는 값이 `S.inq`이기 때문이다
  (목표치 `PERF_GOAL.inq=50`은 그대로).
- **주간회의 「도입문의」는 바꾸지 않았다**(사용자: "원래 집계가 잘되어있었네 고칠필요없어") —
  `wkAutoPerf`의 그 칸은 종전대로 **전체 문의와 같은 기준**이다. 두 화면의 이름이 다르므로,
  주간 「도입문의」와 실적 분석 「전체 문의」가 같은 값이라는 점을 기억한다.

### 19-1. 통합현황 「지난 업로드 대비」 증감 (v08.327 → v08.350)

사용자 요청("통합현황의 모든 숫자들이 과거랑 비교했을때 늘었다는건지 줄었다는건지 알수가 없어")
+ 사용자 확정 **「지난 업로드 대비」**. 같은 그릇(`__insSnap` / STATE_MAP `ins_snap`)에 필드만 더한다.

- **적립**: `_homeSnapPut(rec)` — **통합현황을 그릴 때** 호출한다(`el.innerHTML=` 바로 앞).
  화면에 찍는 값 그대로를 넣으므로 카드 숫자와 스냅샷이 어긋날 수 없다.
  · 값이 **실제로 달라졌을 때만** 저장한다 — 렌더마다 `IX_saveToSheet`를 부르지 않는다.
  · `null`·비숫자는 **기록하지 않는다**(0으로 채우면 다음 업로드에서 가짜 증감이 생긴다).
  · 필드: `hTot`(누적 가입) `hChu`(누적 해지) `hPau`(누적 정지) `hNor`(현재 정상)
    `hPlus`·`hPlusR`(플러스 곳수·전환율) `srTot`·`srJn`·`srHj`(거래처 3종).
- **날짜별 표**: `_homeDeltaTable()` — 통합현황 맨 아래 접이식(`details.fold`, **기본 닫힘**).
  **행 = 지표 12종 · 열 = 업로드한 날(최근 10회, 왼쪽이 오래된 날) · 맨 오른쪽 = 마지막 두 기록의 증감.**
  · v08.330에서는 「그때 값 / 지금 값 / 증감」 3열이었는데 **v08.345에서 날짜를 열로 폈다**
    (사용자 요청 "날짜별 지표를 보여줘") — 사이 흐름이 안 보였기 때문이다.
  · 그 날 기록이 없는 칸은 **「—」**. 0으로 지어내지 않는다 — 그 날 통합현황을 안 열었으면
    통합현황 필드(`hTot`·`srTot` 등)는 비어 있고 인사이트 필드만 있는 날이 생긴다.
  · 값이 **한 번도** 없는 지표는 줄을 만들지 않는다. 스냅샷이 하나도 없으면 블록 자체를 안 만든다.
- **표시**: `_homeDelta(key, unit, dec, onDark)` — 「▲ +6곳 지난 업로드 2026-09-10 대비」.
  비교할 스냅샷이 2개 미만이면 「증감은 다른 날 파일을 한 번 더 올리면 표시됩니다」.
  `onDark=1`은 플러스 전환율 hero(남색 배경) 전용 — 기본 회색은 그 배경에서 안 읽힌다.
- **두 함수는 `_insSpark`·`_insMonthlyHTML`과 같은 스코프**에 둔다(v08.320에서 다른 스코프에
  정의해 홈 화면 전체가 죽은 적이 있다 — 그 교훈).

#### v08.350 — 「증감은 최근 업데이트 전과 후를 비교해줘」 (스냅샷 = 날이 아니라 **업데이트 사건**)

사용자 지적: 증감이 늘 「— 0곳」으로 보였다. 원인 두 가지였다.

1. **값이 그대로여도 날마다 칸이 생겼다.** 종전 `_homeSnapPut`은 「오늘 레코드」와만 비교했는데,
   새 날에는 오늘 레코드가 없으니 모든 값이 「달라짐」으로 잡혀 무조건 저장됐다 → 마지막 두 칸이
   같은 값이 되어 증감 0.
2. **같은 날 두 번 올리면 앞 값이 지워졌다.** 레코드 키가 날짜뿐이라 덮어썼다.

고친 방식 — **공용 헬퍼 3종(전역)**, `xlParseOffice` 바로 위에 정의:

| 함수 | 하는 일 |
|---|---|
| `window.__snapLatest(arr,key)` | 전체 기록에서 그 지표의 **마지막 값**을 찾는다(바뀌었는지 판단) |
| `window.__snapNowT()` | `HH:MM` — 레코드의 `t`(같은 날 안에서의 순서) |
| `window.__snapSort(arr)` | `d`+`t`로 정렬 — 날짜만으로 정렬하면 같은 날 순서가 안 맞는다 |
| `window.__snapPush(arr,rec)` | 마지막 칸이 **오늘·10분 이내·겹치는 값 없음**이면 그 칸에 합치고, 아니면 새 칸. 정렬·60개 보관까지 담당 |

- **`__snapPush`의 10분 병합창이 필요한 이유**: 적립 지점이 **두 곳**이다 —
  ① 세무사무소목록 업로드(`xlParseOffice` 안, 인사이트 3종 `all`·`fund`·`tax`)
  ② 통합현황 렌더(`_homeSnapPut`, 통합현황 지표 9종).
  업로드하면 곧바로 화면이 다시 그려지므로 **둘은 같은 한 번의 업데이트**다. 그냥 각각 칸을 만들면
  같은 시각에 반쪽짜리 칸이 두 개 생겨 표가 읽히지 않는다(실측으로 확인하고 넣은 장치).
  같은 필드를 **다른 값으로** 다시 쓰는 것이면 병합하지 않고 새 칸을 만든다(= 진짜 새 업데이트).
- **레코드 필드 추가**: `t`(HH:MM) · `ts`(epoch ms, 병합창 판정용). 옛 레코드에는 없다 —
  `ts`가 없으면 병합하지 않으므로 그대로 둬도 안전하다.
- **보관 개수 36 → 60** (하루에 여러 칸이 생길 수 있어서).
- **`_insMonthlyHTML` 동반 수정**: 레코드가 「인사이트만 든 칸」/「통합현황만 든 칸」으로 갈릴 수
  있어, 달의 마지막 칸을 잡기 전에 **인사이트 값이 든 칸만** 추린다(안 그러면 월별 표가 「—」가 된다).
- **`_homeDelta`·`_homeDeltaTable` 정렬도 `__snapSort`로 통일** — 날짜만 비교하던 것을 `d`+`t`로.
- 표 머리글: 같은 날이 두 칸 이상일 때만 그 칸들에 **시각**을 덧붙인다(매번 적으면 지저분).
  증감 열 머리글 아래에 「최근 업로드 전 → 후」를 적어 무엇과 무엇을 비교한 값인지 밝힌다.
- 검증(실측 4종): ①업로드 1회 → 칸 1개(인사이트+통합현황 합쳐짐) ②화면만 3회 더 그려도 칸 그대로
  ③같은 파일 재업로드 → 칸 그대로 ④병합창을 지나 값이 다른 파일 업로드 → 같은 날 두 번째 칸 생성,
  증감이 그 두 칸의 차이.
- 누적 가입·해지·정지는 날짜로 과거를 되살릴 수도 있지만, 현재 정상·전환율·거래처수는 「지금 값」이라
  재계산이 불가능하다 → **한 화면에서 기준이 갈리지 않게 전부 스냅샷으로 통일**했다(사용자 확정).

### 20-2. 목록 섹션 공통 컴포넌트 `lsReg`/`lsHTML` — 「보류 접기」 (v08.356)

팀 탭·주간회의의 목록 섹션 12종은 **설정(cols)만 바꿔 쓰는 공통 컴포넌트** `lsReg(cfg)` +
`lsHTML(id)`로 만들어져 있다(v05.05). 데이터는 `window[cfg.sv]`(예: `__mktIssues`),
저장 키는 `cfg.kind`, 다시 그리기는 `lsRe(id)` → `RENDER[cfg.tab]()`.

**v08.356 요청**("논의사항에는 당장 논의할 내용만 보이고 보류만 모아서 따로 볼수 있게함")

| 설정 | 뜻 |
|---|---|
| `hold:'보류'` | 그 값인 행을 **본 표에서 빼고** 아래 접이식으로 모은다 |
| `holdF` | 볼 필드(기본 `'st'`) |

- `hold`가 **없는 섹션은 종전과 완전히 동일**하다(`A = A0`). 지금 켜 둔 곳은 논의사항 3종
  (`mktIssue`·`uxIssue`·`bizIssue`)뿐.
- 열림 상태는 `window.__lsHold[id]`에 섹션별로 기억한다(**기본 닫힘**). 토글은 `lsHoldToggle(id)`
  — `keepScroll`로 감싸 스크롤 위치가 튀지 않게 한다(`lsEdit`/`lsCancel`과 같은 방식).
- 행 HTML은 `mkRows(list, emptyTx)` 하나로 만들어 **본 표와 보류 표가 같은 코드를 쓴다**
  (수정 중 행·⋯ 메뉴·상태 드롭다운이 두 표에서 똑같이 동작한다).
- 상태 드롭다운을 「보류」로 바꾸면 `lsSet` → `lsRe`가 바로 다시 그려 **그 줄이 즉시 아래로 내려간다.**
- 섹션 제목 배지: 본 표 건수 + `· 보류 N`(보류가 있을 때만).
- 빈 문구도 바꿨다 — 「등록된 논의사항이 없습니다」 → **「지금 논의할 안건이 없습니다」**
  (보류만 남았을 때 「없다」고 하면 오해한다).
- ⚠ **주간회의의 「팀 논의사항 · 요구사항」 블록은 손대지 않았다** — 그쪽은 `lsHTML`이 아니라
  주간회의가 직접 모아 그린다(`aissN` 부근). 거기서도 보류를 빼려면 따로 고쳐야 한다.

#### ⚠ 「논의사항」은 팀 탭에 **두 개** 있다 (v08.356 → v08.357)

v08.356에서 위 표(`lsHTML`)에 보류 분리를 넣었더니 사용자가 *"보류가 그대로 있는데?"* 라고
했다. 사용자가 보던 것은 **보드 바로 아래 카드형 트레이**였다. 이름이 같아 헷갈리니 구분해 둔다.

| 어디 | 함수 | 데이터 | 생김새 |
|---|---|---|---|
| 보드 아래 **트레이**(위) | `paintTray()` · `#oList`·`#oCnt`·`#doneBtn`·`#holdBtn` | `ISSUES`(카드에 붙은 논의) + `UNIS`(미배정) — `allIssues()` | 가로로 늘어선 **카드**, 상태 드롭다운 |
| 그 아래 **표**(밑) | `lsHTML('mktIssue' 등)` | `__mktIssues`·`__uxIssues`·`__bizIssues` | **표**, 안건·제기자·상태·결론 열 |

- **트레이(v08.357)**: 「완료」와 같은 방식으로 **「보류」도 기본 목록에서 뺀다**.
  `SHOWHOLD` 플래그 + `toggleHold()` + 머리글 [보류 보기(N)] / [보류 숨기기].
  두 버튼(보류·완료)은 **서로 독립**이다. 보류 카드도 `.done` 클래스를 붙여 흐리게 보인다.
  빈 목록 문구는 **숨겨 둔 것이 무엇인지** 정확히 적는다(「보류 N건은 [보류 보기]로」).
- **표(v08.356)**: `hold:'보류'` 설정 + 아래 접이식. 위 설명 참고.
- 둘 다 고쳐 뒀으므로, 다음에 「논의사항」 관련 요청이 오면 **어느 쪽인지 먼저 확인**할 것.

### 21-1. 개업세무사 가입을 **주 단위**로 세는 법 (v08.354)

원천의 **프로모션 시작월은 `YYYYMM`뿐**이라 주로 쪼갤 수 없었다 — v08.305에서는 주 종료일이
속한 **달 전체**를 세고 라벨에 「(월)」을 붙여 두었다(주가 두 달에 걸칠 때 두 달이 합산되던
버그를 막은 임시 처리).

**실데이터에서 찾은 규칙**: 프로모션 시작월은 **항상 그 사무소의 어떤 사건이 일어난 달과 같다.**
2026-09-23 파일, 개업세무사 **16곳 전수**:

| 시작월과 같은 달의 사건 | 곳 수 |
|---|---|
| 가입월 | 4 |
| 재가입월 | 1 |
| 요금제변경월 | 10 |
| 인사이트가입월 | 1 |
| **어느 것도 아님** | **0** |

그래서 그 사건의 **날짜를 프로모션 시작일로 못 박는다**.

| 함수 | 하는 일 |
|---|---|
| `window.pkgStartDay(x)` | 시작월과 같은 달인 사건 날짜를 돌려준다(우선순위 **가입 > 재가입 > 요금제변경 > 인사이트가입**) |
| `window.pkgStartCountWeek(s,e)` | 그 날짜가 기간 안인 곳을 센다 — **주간 실적 요약 전용** |

- 못 박은 날짜는 **항상 시작월 안**이므로 **월 합계가 종전과 같다**. 실측 검산(월 기준
  `pkgStartCount` vs 일 기준 `pkgStartCountWeek`): 6월 1=1 · 7월 5=5 · 8월 7=7 · 9월 3=3.
- **월별 화면은 종전 `pkgStartCount`(월 기준)를 그대로 쓴다.** 월 호출부가 `em+'-01' ~ em+'-28'`
  같은 관용구를 쓰고 있어(월 비교라 일자가 무의미했다), 그쪽을 날짜 비교로 바꾸면 **29~31일이
  빠진다**. 손대지 말 것.
- 어느 사건과도 안 맞는 곳이 생기면 **날짜를 지어내지 않고 그 곳만 주간 집계에서 뺀다**
  (null로 화면을 무너뜨리지 않는다). 그런 곳이 있으면 콘솔에 경고를 남긴다.
- 마스터에 `pmNm`·`pmS` 컬럼이 없으면 `null`(화면 「—」) — 청구 파일 기준(2순위)에는 날짜가 없다.
- 라벨 `WKKPI_LABEL`의 「(월)」 표기를 뗐다.

## 22. 주간 업무 현황의 과제 줄·📎 (v08.317 · v08.329)

`teamWeekTableHtml(team)` 안 `rowsFor(wk)` — 그 주에 **실행기록(LOG)이 찍힌 카드**만 나온다
(`wkCardsFor`). 계획을 미리 보여주는 칸이 아니다.

- **과제 줄 클릭** → `window.wkGoCard(tab, cardId)`(전역 — onclick 속성에서 부른다):
  `goTab(tab)`으로 그 팀 탭으로 옮긴 뒤 **`openPage(cardId)`로 카드 상세 패널까지 연다**(v08.329).
  · v08.329 전에는 `goTab(team.id)`만 해서 팀 탭까지만 갔다 — 어느 과제였는지 다시 찾아야 했다.
  · 카드가 없으면 탭 이동만 한다. 기준 월에서 제외된 카드라도 상세는 열린다.
- **📎(`.ww-clip`)** = 그 카드의 자료 링크(`DOCS[카드id]`) 중 `http(s)` URL이 있는 것만.
  · 누를 때 카드 이동이 따라오지 않도록 `event.stopPropagation()`(v08.317).
  · v08.329에서 **청록 칩**(19×19, `#EAF9F8` + `#A9E3E0` 테두리, hover 시 `--teal-dk`)으로 바꿨다 —
    종전엔 반투명 이모지라 링크 있는 과제인지 눈에 띄지 않았다.

## 23. 개선의견 (v08.336 · 알림 v08.338)

설정 모달(`#setModal` → `paintSettings`) 안의 **「개선의견」 탭**. 팀원이 쓰면서 느낀 점을 남기고
관리 계정이 상태·회신을 관리한다.

- 저장: `STATE_MAP`의 **`ideas`** → `window.__ideas`
  = `[{id, d(등록일시), by(이메일), tx(내용), st(상태), rp(회신)}]`, 최신 글이 배열 앞.
- 함수: `ideaPanelHtml()`(표) · `window.ideaAdd/ideaSet/ideaDel` · 상태 4종 `IDEA_ST`.
  `ideaSet`은 회신(rp) 입력 중에는 **다시 그리지 않는다**(상태 st만 재렌더 — 배지 색이 바뀌므로).
- **권한**(관리 계정 = `DATA_UPLOAD_EMAIL`):
  | | 관리 계정 | 그 밖의 팀원 |
  |---|---|---|
  | 상단바 버튼 | 「설정」 | **「개선의견」** |
  | 설정 탭 | 사용자 · 부서 · 개선의견 | **개선의견만** |
  | 상태·회신 | 바꿀 수 있음 | 읽기만 |
  | 지우기 | 모든 글 | 본인 글만 |
- ※ **「데이터 업로드 메뉴·설정(사용자·부서) 관리 화면은 관리 계정 전용」이라는 규칙은 그대로다** —
  v08.336에서 연 것은 **개선의견 하나뿐**이다(접수는 누구나 할 수 있어야 뜻이 있다).
  `openSettings(tab)`은 인자로 탭을 지정할 수 있고, 관리 계정이 아니면 자동으로 `idea` 탭을 연다.

### 23-1. 개선의견 알림 (v08.338)

서버에서 메일을 보낼 수단이 없으므로, **업데이트 내역 NEW 배지(`updIsUnread`/`updMarkRead`)와
같은 구조**로 화면 안에서 알린다.

- 각 항목에 **`u`(마지막 변경 시각, 초까지) · `ub`(바꾼 사람)** 를 남긴다 —
  등록·상태 변경·회신을 한 값으로 잡는다. 화면 표시용 `d`는 분까지만.
  ⚠ **분까지만 쓰면 「읽은 뒤 같은 분에 달린 회신」을 놓친다**(검증 중 실제로 나온 문제) → 초까지 쓴다.
- 읽은 시점은 사람별로 `__userDir[이메일].ideaRead`(= `saveToSheet('user_dir')`)에 둔다.
- `window.ideaUnreadList()` = `ub !== 나` 이면서 `u > ideaRead` 인 항목.
  `u`가 없는 옛 기록은 `d`로 비교되어 **이미 읽음**으로 본다(없는 알림을 만들지 않는다).
- 표시: 상단바 버튼 안 `#ideaBadge`(`.idea-b` 빨간 배지) — `ideaPaintBadge()`가 **배지만** 갈아 끼운다
  (버튼 전체를 다시 그리면 로그아웃 버튼까지 새로 만들어진다).
- 로그인 직후 `ideaNotify()`가 배지 + 토스트를 한 번 띄운다. **실시간 푸시는 아니다** — 화면을
  새로 열거나 새로고침할 때 갱신된다.
- 읽음 처리: 개선의견 탭을 그릴 때 `setTimeout(...,0)`으로 `ideaMarkRead()`(렌더 중 저장이 끼어들지 않게).

## 24. 화면 디자인 통일 — 주간회의(flow) 스타일 (v08.339 · v08.341)

사용자 요청("탭별로 상이한 디자인을 하나의 디자인으로 통일 — 기준은 주간회의탭 flow 스타일")에
따라 **CSS만으로**(A안 확정) 모양을 맞췄다. **HTML·렌더 함수는 건드리지 않았다.**

- **어디에 있나**: `#ixHost .kpi-grid` 정의 바로 위의 주석 묶음 두 개
  (① `#ixHost .sect …` — IX 화면 전체 / ② `#page > .panel, #page .act-list` — IX 밖 단순 화면).
  **되돌리려면 그 두 블록만 지우면 된다.**
- **기준값**(주간회의 `.keytask`·`.kt-h`에서 그대로):
  카드 = 1px 테두리 + 왼쪽 4px `#00C896` + radius 8px + `0 2px 6px rgba(0,0,0,.06)` + padding 14px 16px /
  제목 = 13px · 800 · `#00876A`
- ⚠ **색을 `var(--teal)`로 쓰면 안 된다** — `#ixHost`가 자기 안에서 `--teal`을 **#1BC5BD**로 다시
  정의해, 변수로 쓰면 주간회의와 **다른 초록**이 나온다(실측 `rgb(27,197,189)`).
  그래서 `#ixHost .sect{--flow-ac:#00C896;--flow-ti:#00876A}` 두 줄로 따로 두고 그것을 쓴다 —
  **색을 바꾸려면 이 두 줄만 고치면 된다.**
- **구조가 두 가지라 규칙도 두 가지**다:
  | 패턴 | 처리 |
  |---|---|
  | `.blk`(통합현황·일별·월별·누적·실적관리) | 이미 컨테이너라 카드 모양만 부여 |
  | `.sec-label` + 바로 다음 형제 | 위/아래로 나눠 한 장처럼 묶음. **그림자는 아래쪽에만**(둘 다 주면 맞닿는 선에 그림자가 보인다) |
  | 제목 없이 놓인 `.sect > .panel` | 카드 모양 부여. `.sec-label + .panel` 쪽이 더 구체적이라 겹쳐도 테두리가 두 번 안 그려진다 |
- **시장 현황(`sales`)도 v08.342에서 맞췄다**(사용자 지적 "시장현황탭만 되게 겉도는거 같은데") —
  `#salesHost .main > section` 8개를 카드로, `.sec-h h2`를 청록 굵은 제목으로, **검은 히어로 배너
  (`header.top`)를 흰 카드로** 바꿨다. 안쪽 `.card`·`.chartbox`는 모서리(8px)만 맞춘다 —
  왼쪽 선을 또 넣으면 청록이 겹쳐 답답해진다.
  · 남은 예외 하나: `#salesHost .banner`(「시장 한눈에 보기」 안의 **검은 띠**). 글자색이 흰색이라
    배경만 밝게 바꾸면 글자가 사라진다. 시장 데이터가 없어 내용을 확인할 수 없어 **그대로 두었다**.
- **일부러 손대지 않은 화면**: **팀 탭(`mkt`·`ux`·`biz`)** — 본체가 칸반 보드라 「블록 카드」 개념이
  맞지 않는다(그 안의 「이달의 핵심과제」는 이미 주간회의와 같은 `.keytask`다).
- **구조상 변화가 거의 없는 화면**: **신청 관리(`inq`)** — 최상위가 전부 class 없는 `div`라
  카드로 감쌀 대상이 없다. 바꾸려면 HTML을 고쳐야 한다(A안 범위 밖).

## 25. 정지업체 — 메모 정렬(v08.342) · 컨택 색 구분(v08.343)

「메모」 열 제목을 눌러 **메모 있고/없고**로 줄을 세운다(`spPaint`의 `L.sort` 안 `k==='memo'` 분기).
글자 순서로 세워 봐야 뜻이 없어 **묶음 두 개**로만 가른다 — 같은 묶음 안에서는 업체명 가나다순.
`▼`(처음 누름) = 메모 있는 곳 먼저 · `▲` = 아직 안 적은 곳 먼저.

⚠ **이 정렬 코드와 글자까지 똑같은 문장이 해지·재가입 화면에도 있다.** 고칠 때는 반드시
`window.spPaint` 함수 안인지 확인할 것 — v08.316에서 실제로 churn 쪽을 잘못 고친 적이 있다.

**함께 고친 버그**: `th()`가 `style` 속성을 **두 번** 내보내고 있었다
(`<th style="width:84px" onclick=… style="cursor:pointer;width:84px">`). HTML은 **먼저 나온 속성**만
쓰므로, 폭을 준 열(대표자·연락처·요금제·정지일·정지해제일)에서는 `cursor:pointer`가 통째로 무시돼
**누를 수 있는 열인지 화면에 드러나지 않았다.** 하나로 합쳐 8개 열 모두 손 모양 커서가 나온다.

### 25-1. 컨택 안 한 곳 색 표시 (v08.343 → v08.344에서 칠하는 쪽을 뒤집음)

**판단 기준은 메모다** — 그 칸은 v08.319부터 「정지 안내 통화 결과를 적는 자리」다.
메모가 있으면 한 번이라도 연락한 곳(컨택 완료), 비어 있으면 아직 안 한 곳으로 본다.

| | 표시 |
|---|---|
| **미컨택**(메모 없음) | 행 배경 `#FFF8EC` + 업체명 칸 왼쪽 `inset 3px #F5A623` 막대 + 업체명 `#A85B00` |
| 컨택 완료(메모 있음) | 흰 배경 그대로 — 끝난 줄은 가라앉는다 |

※ **v08.343에서는 반대로 컨택한 쪽을 칠했다가 사용자 지시("컨택안한곳에 색칠")로 뒤집었다** —
이 화면은 「남은 곳에 전화를 거는」 화면이라 **남은 줄이 칠해져 있는 편**이 일하기 쉽다.

- 행에 붙는 class는 **`tr.sp-todo`**(`spPaint`에서 `x.memo`가 **비었을 때** 붙인다).
- **메모를 적으면 그 줄의 색이 즉시 사라진다** — `spMemoSet(key, v, el)`이 `el.closest('tr')`의
  class를 `toggle('sp-todo', !t)`로 바꾼다(메모를 지우면 다시 칠해진다). **목록 전체를 다시 그리면 안 된다**(입력 칸이 새로 만들어져 커서·한글
  조합이 끊긴다 — 공통 규칙). 그래서 세 번째 인자로 `this`를 넘긴다.
- 목록 위 카운트는 `window.spCntHtml()` — 「컨택 N곳 · 미컨택 N곳」(색 구분의 **범례 역할**도 한다).
  종전 「메모 N건」을 대체했다.
- 색만 쓰지 않고 **막대를 함께** 둔 이유 — 색을 구분하기 어려운 사람도 알아볼 수 있어야 한다.
- ⚠ 색은 **값을 직접 적는다**(`#FFF8EC`·`#F5A623`·`#A85B00`). `#ixHost` 안에서 `--teal`이 #1BC5BD로
  재정의돼 있어 변수로 쓰면 다른 초록이 나온다 — v08.341에 이어 **또 걸린 함정**이라 실측으로 확인했다.

### 13-4. 「전체 가입 현황」 = 시스템 「세무사무소현황」과 대조하는 표 (v08.348 → v08.349)

사용자 확정: **"전체가입현황은 가입수,재가입수,정지수,해지수를 보여줘"**(v08.348) — 이 표는
회의에서 시스템 자료와 **나란히 놓고 그대로 대조하는 표**다. 그래서 네 줄만 둔다.
이어서 **"재가입수를 가입수에 포함해서 총 가입수로 표현해줘"**(v08.349) — 첫 줄을 합계로 바꿨다.

| 행 | 값 | 기준 컬럼 |
|---|---|---|
| **총 가입수** | `join_n` + `join_r`을 **더해서** 만든다 | 가입일시 + 재가입일시 |
| └ 재가입 | `join_r` (보조 줄, `color:var(--muted)`) | 재가입일시 |
| 정지수 | `pause` | 정지일시 |
| 해지수 | `churn` | 해지일시 |
| **순증** | `총 가입수 − 해지수`(화면에서 계산, `rows.nt`) | — |

- **총 가입수는 `joinSplitCount('join')`을 쓰지 않는다.** `join`은 `jn||rj`로 **행**을 세므로
  한 달에 가입·재가입이 둘 다 있는 곳을 한 번만 센다 → 「총 가입수 ≠ 가입수+재가입수」가 된다.
  표에서 두 줄이 어긋나 보이면 안 되므로 `rows.jn[m]+rows.rj[m]`으로 **더한다**(`rows.tj`).
- 재가입 줄을 남긴 이유: 시스템 「세무사무소현황」의 **재가입수와 계속 대조**할 수 있어야 해서.

- **현재 상태로 걸러내지 않는다** — 그 뒤에 해지·재가입한 곳도 그 달에 그대로 남는다(v08.305와 같은 원칙).
- `pause`·`churn`은 **가입 모수와 무관**하므로 `rows` 필터를 거치지 않고 **파일 전체(`R`)에서** 센다
  (`pkg`를 위에서 먼저 처리하는 것과 같은 이유). 위치는 `chg` 정의 바로 아래.
- **2026 실데이터 전량 대조 완료**(2026-09-23 파일, 916행): 가입 **86** · 재가입 **11** ·
  정지 **91** · 해지 **115** — 9개월 × 4지표 모두 시스템 「세무사무소현황」과 일치.
  v08.349 화면 실측: 총 가입수 **97**(=86+11) · └ 재가입 **11** · 정지 **91** · 해지 **115**.
- **플러스·리포트·개업세무사·요금제 상향은 위 「월별 실적 현황」 표가 담당한다.**
  v08.346~v08.347에서 이 표에 얹었다가, 시스템과 맞대는 표라는 성격이 분명해져 덜어냈다
  (그때 만든 `join`·`up`·`hasOnly` kind는 함수에 남아 있고 **지금 부르는 화면은 없다**).
- **순증 행(v08.354 요청 「맨 마지막에 순증개수를 추가해」)**: `총 가입수 − 해지수`.
  이 파일이 쓰는 순증 산식(신규가입 + 재가입 − 해지 — `wkAutoPerf`·실적 분석과 동일)과 같다.
  **정지는 빼지 않는다**(되돌아올 수 있는 상태). 부호를 붙이고 플러스 청록 / 마이너스 빨강으로
  칠하는 전용 셀(`netCell`)을 쓴다 — 다른 행과 달리 음수가 나오기 때문.
  2026 실측: +5 · +3 · −1 · −10 · −2 · −7 · −9 · +1 · +2 = **−18**.
